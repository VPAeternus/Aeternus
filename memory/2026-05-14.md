## 2026-05-14

- Task 1 cleanup only.
- Default no-`--master-universe` path now materializes active master universe after Gate 1 finalized-output check and before Gate 2 missing-path validation.
- `master_source.py` now wraps malformed additions JSONL as `ValueError` with ledger path and line number.
- Malformed start JSON shape now includes start path in error.
- Verification: `python3 -m pytest tests/test_cli_fundamental_run_today.py tests/test_fundamental_daily_master_source.py tests/test_fundamental_daily_universe_contract.py -q` -> `19 passed`.

- Task 2 daily input guardrails are complete in the feature worktree.
- `fundamental-run-today` now has `--allow-missing-handoff` and `--allow-date-quarter-mismatch`.
- Broad-final hard-stops when the daily handoff is missing or date/quarter do not match unless explicitly allowed.
- Diagnostic-only can continue without handoff and reports `handoff_missing`; scout-smoke requires handoff unless explicitly allowed.
- Verification: `python3 -m pytest tests/test_cli_fundamental_run_today.py tests/test_fundamental_daily_orchestrator_contract.py -q` -> `22 passed`.

- Finished remaining fundamental daily main orchestration implementation directly, without subagents.
- Main stock list flow now resolves new dealflow tickers before Gate 2, rejects unresolved names before scoring, writes pending additions, and emits a full current-quarter combined master JSON.
- Later SEC coverage now uses the combined current-quarter JSON, not the old starting master file.
- Main scoring price step is cache-first and writes newly fetched prices to run/shared caches.
- Companyfacts fallback copies shared SEC `facts_TICKER.json` into live daily companyfacts before coverage/scoring.
- LLM evidence recovery now runs inside the daily flow: uses shared SEC raw document caches, materializes recovered docs, and only calls SEC fetch when a fetch queue/service exists.
- Added plain `daily_ticker_status.csv` status/reason file and coverage gating into Top10 + Plus5 + shadow publish.
- Verification: focused daily suite `60 passed`; broader daily/review/CLI suite `99 passed`; py_compile on touched daily-run modules passed.

- Patched final review findings for daily orchestration.
- Broad-final Gate 7 now stops when LLM-required rows still lack usable earnings evidence after recovery.
- Persistent additions ledger now writes only resolved new dealflow tickers that reached final scored rows after successful publish.
- Verification: targeted regressions passed; full daily/review/CLI/architecture suite `114 passed, 2 warnings`; py_compile passed; `git diff --check` passed; final `codex review --uncommitted` found no actionable defects.
