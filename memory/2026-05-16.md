# 2026-05-16

## 2023Q1 Walkforward Finalized

- Ran next clean walkforward quarter: `2023Q1`, using `2022Q4` final scores as prior comparison data.
- Run root: `tradingagents/research/fundamental/runs/2023-03-31/2023Q1/walkforward_clean_v1`.
- First pass fetched `3,415` SEC docs into `live_sec`; later passes confirmed most remaining queue items were cached. Final Gate 4 blocker was the known `BHFAO` / `BHFAP` preferred-share alias issue.
- Fixed `BHFAO` and `BHFAP` alias docs by copying the already-fetched `BHF` filing docs under preferred-share ticker names.
- LLM need: `554` packets total (`280` current, `274` prior recovery); `6` prior names were impossible because no prior earnings filing exists.
- Reused `410` exact saved LLM rows and completed only the `144` missing rows (`74` current `2023Q1`, `70` prior `2022Q4`).
- Fixed `clean()` regression from Q4 work: string `"none"` must stay valid text because LLM demote severity uses `"none"`; only real `NaN`/`null` should become blank.
- Combined post-LLM file: `tradingagents/research/fundamental/runs/2023-03-31/2023Q1/walkforward_clean_v1/post_llm_scores_completed_2023Q1_all_packets.csv`, `554` unique sample IDs.
- Clean final run passed Gates `1-11`. Final rows `1,206`; LLM-complete rows `280`; QoQ missing rows `0`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- Complete panel validation passed with `0` errors.
- PIT master appended `1,206` rows; total PIT rows now `6,011` across `2022Q1`-`2023Q1`.

## 2022Q4 Walkforward Finalized

- Ran next clean walkforward quarter: `2022Q4`, using `2022Q3` final scores as prior comparison data.
- Run root: `tradingagents/research/fundamental/runs/2022-12-31/2022Q4/walkforward_clean_v1`.
- First pass fetched `3,409` SEC docs into `live_sec` because the old raw SEC HTML cache had been deleted and live cache did not yet contain all Q4 documents.
- Fixed `BHFAO` and `BHFAP` alias docs by copying the already-fetched `BHF` filing docs under the preferred-share ticker names.
- Initial LLM need: `324` packets; reused `264` exact saved LLM rows and completed only `60` missing rows.
- QA found stricter prior-quarter rule gap: partial prior LLM defaults like `post_llm_demote_severity=none` must not count as a complete prior LLM review.
- After patch, Q4 required `317` prior `2022Q3` LLM recovery rows; reused `520 / 639` total exact saved rows and completed `119` missing prior rows.
- QA found price-cache gap: cached `NaN` open prices were treated as usable, so `GBTG` prior QoQ price did not refetch. Patched common clean/missing checks and price-cache usability so `NaN` is missing.
- Clean final run passed Gates `1-11`. Final rows `1,206`; LLM-complete rows `322`; QoQ missing rows `0`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- Complete panel validation passed with `0` errors.
- PIT master appended `1,206` rows; total PIT rows now `4,805` across `2022Q1`-`2022Q4`.
- Focused verification: `tests/test_fundamental_daily_scoring_inputs.py`, `tests/test_fundamental_daily_price_cache.py`, and prior-LLM recovery test passed: `13 passed`.

## Clean 2022Q1 Walkforward Start

- Agreed rule: `2021Q4` is comparison-only; first official scored walkforward quarter is `2022Q1`.
- Created comparison-only file: `tradingagents/research/fundamental/runs/2021-12-31/2021Q4/walkforward_clean_v1/qoq_context_only_2021Q4.csv`.
- Comparison file has `1,251` rows and `114` reused saved LLM rows.
- Ran `2022Q1` clean from `master_fundamental_universe_start_2021Q4.json` with the 2021Q4 comparison file.
- No new LLM extraction ran.
- Gates 1-7 passed.
- Key counts: universe `1,251`, pre-LLM rows `1,251`, tier rows `1,197`, current LLM-required rows `170`, entry price quarantine `6`, score-input quarantine `48`.
- Prior LLM recovery: `57` prior 2021Q4 rows needed; `5` marked impossible because no prior earnings filing exists; `0` prior evidence rows still missing after patch.
- Reused saved LLM rows: `151 / 227` total packets.
- Gate 8 hard-stopped because `76` exact ticker-quarter LLM rows are missing.
- Missing file: `tradingagents/research/fundamental/runs/2022-03-31/2022Q1/walkforward_clean_v1/reused_post_llm_missing_2022Q1.csv`.
- Reused source file: `tradingagents/research/fundamental/runs/2022-03-31/2022Q1/walkforward_clean_v1/reused_post_llm_sources_2022Q1.csv`.
- Code patch: prior-quarter rows with no earnings filing and missing periodic metadata now count as impossible, not recoverable missing evidence.
- Verification: focused prior recovery test passed; `py_compile` on `orchestrator.py` passed.

## 2022Q1 Walkforward Finalized

- Completed only the `76` missing LLM rows needed for `2022Q1`; reused the other `151` exact ticker-quarter LLM rows.
- Combined post-LLM file: `tradingagents/research/fundamental/runs/2022-03-31/2022Q1/walkforward_clean_v1/post_llm_scores_completed_2022Q1.csv`, `227` unique sample IDs.
- Fixed a scoring bug: reused LLM rows can no longer overwrite fresh price/date/base score fields.
- Fixed first-official-quarter QoQ rule: if prior earnings filing is clearly impossible, that one missing prior comparison does not block final scoring.
- Clean final run: `tradingagents/research/fundamental/runs/2022-03-31/2022Q1/walkforward_clean_v2`.
- Gates `1-11` passed. Final rows `1,197`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- `NRDS` is allowed missing prior QoQ because prior earnings filing was impossible.
- Complete panel validation passed with `0` errors.
- PIT master appended: `outputs/fundamental_backtest/pit_master/fundamental_pit_master.csv`, appended `1,197` rows for `2022Q1`.

## 2022Q2 Walkforward Finalized

- Ran next clean walkforward quarter: `2022Q2`, using `2022Q1` final scores as prior comparison data.
- First pass fetched `3,382` SEC docs into `live_sec`.
- Fixed `BHFAO` and `BHFAP` alias docs by copying the already-fetched `BHF` filing docs under the preferred-share ticker names.
- LLM need: `301` packets total, `300` current-quarter and `1` prior-recovery packet.
- Reused `233` exact ticker-quarter saved LLM rows; completed only the `68` missing `2022Q2` LLM rows.
- Combined post-LLM file: `tradingagents/research/fundamental/runs/2022-06-30/2022Q2/walkforward_clean_v1/post_llm_scores_completed_2022Q2.csv`, `301` unique sample IDs.
- Clean final run: `tradingagents/research/fundamental/runs/2022-06-30/2022Q2/walkforward_clean_v1`.
- Gates `1-11` passed. Final rows `1,198`; LLM-complete rows `300`; QoQ missing rows `0`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- Complete panel validation passed with `0` errors.
- PIT master appended: `outputs/fundamental_backtest/pit_master/fundamental_pit_master.csv`; total PIT rows now `2,395`.

## 2022Q3 Walkforward Finalized

- Ran next clean walkforward quarter: `2022Q3`, using `2022Q2` final scores as prior comparison data.
- First pass fetched `3,395` SEC docs into `live_sec`.
- Fixed `BHFAO` and `BHFAP` alias docs by copying the already-fetched `BHF` filing docs under the preferred-share ticker names.
- LLM need: `268` current-quarter packets.
- Reused `213` exact ticker-quarter saved LLM rows; completed only the `55` missing `2022Q3` LLM rows.
- Combined post-LLM file: `tradingagents/research/fundamental/runs/2022-09-30/2022Q3/walkforward_clean_v1/post_llm_scores_completed_2022Q3.csv`, `268` unique sample IDs.
- Clean final run: `tradingagents/research/fundamental/runs/2022-09-30/2022Q3/walkforward_clean_v1`.
- Gates `1-11` passed. Final rows `1,204`; LLM-complete rows `268`; QoQ missing rows `0`; allowed prior-impossible QoQ rows `2`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- Complete panel validation passed with `0` errors.
- PIT master appended: `outputs/fundamental_backtest/pit_master/fundamental_pit_master.csv`; total PIT rows now `3,599`.

## 2023Q2 Walkforward Finalized

- Ran next clean walkforward quarter: `2023Q2`, using `2023Q1` final scores as prior comparison data.
- Final run root: `tradingagents/research/fundamental/runs/2023-06-30/2023Q2/walkforward_clean_v2`.
- Gate 4 stopped on the known `BHFAO`/`BHFAP` preferred-share alias docs; copied matching `BHF` filing docs for accession `0001685040-23-000026` under the alias ticker filenames and reran.
- Initial `v1` run over-requested prior LLM review because legitimate `0` flags were treated as blank. Patched prior LLM completeness to use `clean()` so `0` counts as present.
- Corrected LLM need after patch: `368` packets total, `281` current-quarter and `87` prior-recovery packets.
- Reused already completed exact LLM rows and filtered them to the corrected packet set.
- Combined post-LLM file: `tradingagents/research/fundamental/runs/2023-06-30/2023Q2/walkforward_clean_v2/post_llm_scores_completed_2023Q2_all_packets.csv`, `368` unique sample IDs.
- Gates `1-11` passed. Final rows `1,208`; post-LLM rows `368`; LLM-complete rows `281`; QoQ missing rows `0`; Top15 rows `15`; shadow rows `15`; shadow refill replacements `0`.
- Complete panel validation passed with `0` errors.
- PIT master updated: `outputs/fundamental_backtest/pit_master/fundamental_pit_master.csv`; total PIT rows now `7,219` with `0` duplicate ticker-quarter rows.

## Pre-2023Q3 Speed Fixes

- Added SEC alias materialization before/after fetch passes. If live cache has parent filing docs, alias ticker filenames are copied automatically from the cached parent docs.
- This targets the repeated `BHFAO`/`BHFAP` preferred-share stop without weakening SEC evidence checks.
- Added shared LLM extraction cache at `/Users/aeternusholdings/Documents/GitHub/AeternusHoldings/cache/sec/llm_extractions/daily_post_llm_cache.csv`.
- Daily LLM now checks the shared cache first. If all packets are cached, it writes the post-LLM CSV and final scoring can continue. If some are cached, only missing packets are sent to LLM.
- Orchestrator saves validated post-LLM rows back into the shared cache after post-file/in-session completion.
- Seeded cache from existing walkforward completed post-LLM files: `1,987` unique sample IDs.
- Verification: focused daily/coverage/LLM tests `62 passed`; `git diff --check` passed.

## Clean 2021Q4 Walkforward Restart

- Deleted bad combined/PIT CSV outputs that reused old historical panel rows.
- Rebuilt `2021Q4` clean from `tradingagents/research/fundamental/data/master_fundamental_universe_start_2021Q4.json`.
- No new LLM extraction run after user instruction.
- Reused prior exact-quarter post-LLM extraction rows into `tradingagents/research/fundamental/runs/2021-12-31/2021Q4/walkforward_clean_v1/reused_post_llm_scores_2021Q4.csv`.
- Reuse result: `114 / 131` LLM-required rows matched.
- Missing exact `2021Q4` prior extraction rows: `AXTI`, `BMEA`, `BTU`, `CCC`, `CIFR`, `DCGO`, `EVER`, `LBTYK`, `LFCR`, `LULU`, `MIR`, `MWA`, `NVDA`, `OII`, `PCG`, `ROST`, `VTRS`.
- Rerun with `--llm-mode post-file` hard-stopped at Gate 8 as expected: post-file had no duplicate, unexpected, or malformed rows, but was missing those `17` sample IDs.
- Missing list: `tradingagents/research/fundamental/runs/2021-12-31/2021Q4/walkforward_clean_v1/reused_post_llm_missing_2021Q4.csv`.
- Reuse source file list: `tradingagents/research/fundamental/runs/2021-12-31/2021Q4/walkforward_clean_v1/reused_post_llm_sources_2021Q4.csv`.

## Fundamental Complete Panel Refresh

- Refreshed combined panel: `outputs/fundamental_backtest/full_complete_panel_2021Q4_2026Q2/fundamental_complete_prellm_to_top15_2021Q4_2026Q2.csv`.
- Used canonical `2021Q4` through `2026Q1` history from `fundamental-history-complete-panel`.
- Replaced `2026Q2` with latest validated daily final run: `daily_postfile_prior_context_fix_v1/fundamental_final_scores_2026-05-15.csv`.
- New counts: `23,619` rows, `330` columns, `1,365` unique tickers, `19` quarters, `1,316` `2026Q2` rows.
- Review fix preserved historical Top15/shadow selected flags and kept current-quarter selections.
- Final validation passed: `0` duplicate ticker-quarter rows, `0` errors, Top15 selected `206`, shadow selected `272`.
- Manifest financial blank counts now match the CSV: revenue `5`, assets `2`, net income / operating cash flow / investing cash flow / financing cash flow `0`.
- Blank flag cells were normalized to `0`, so validator and sidecars agree.
- Current-quarter Top15 rows now keep selector metadata: global rank, variant, treatment, operating setting, and reason-code fields.
- Source-file provenance for mirrored `2026Q2` rows now points to repo-local `source_artifacts/`, not machine-local worktree paths.
- Wrote canonical `fundamental_complete_prellm_to_top15_2026Q2.*` sidecars and copied them to range-named compatibility files.
