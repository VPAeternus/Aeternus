# 2026-05-13

## Fundamental SEC review-list filter

- Implemented the plain SEC-to-review-list flow.
- New file: `tradingagents/research/fundamental/src/daily_run/review_list_filter.py`.
- New daily-run switch: `fundamental-run-today --build-review-list-from-sec`.
- Flow starts from SEC company tickers, then keeps only stocks with:
  - required earnings `8-K`
  - required press-release exhibit
  - required periodic filing
  - required core company data
  - raw Yahoo OHLCV
  - raw close `>= 2`
  - `ADV60 >= 500,000` shares
- Outputs:
  - `review_stock_list_<quarter>.csv`
  - `review_stock_list_rejections_<quarter>.csv`
  - `review_stock_list_summary_<quarter>.json`
- Price checks now check cached parquet data first from `/Users/aeternusholdings/.cache/autoresearch_fundamentals`.
- If SEC/company data is ready but price cache is missing, the review-list builder fetches Yahoo prices and saves a `review_price_cache_<quarter>_<date>.parquet` file for reuse.
- Yahoo price fetch is only attempted for names that first pass SEC evidence and company-data checks, not all 10,348 SEC tickers.
- Cache-only 2026Q2 count result:
  - SEC map names: `10,348`
  - strict SEC-ready names: `1,148`
  - company-data-ready names: `1,132`
  - cached-price names: `1,115`
  - final after raw close and ADV60: `791`
  - count summary: `eval_results/fundamental/2026-05-13_2026Q2_sec_review_filter_cache_only/cache_only_count_summary.json`
- Verification:
  - red tests failed first on missing module/config
  - `tests/test_fundamental_review_list_filter.py -q` -> `8 passed`
  - `tests/test_fundamental_daily_*.py tests/test_fundamental_review_list_filter.py -q` -> `48 passed, 2 warnings`
  - touched `py_compile` passed
  - CLI help shows new review-list flags
- Full cache-only review-list run completed with no live SEC/Yahoo fetch:
  - output root: `eval_results/fundamental/2026-05-13_2026Q2_review_list_cache_test_v2`
  - SEC map names: `10,348`
  - kept review-list names: `791`
  - rejected after real rule checks: `524`
  - pending fetch-needed names: `9,033` (`8,733` SEC fetch, `243` document fetch, `57` price fetch)
  - gates 1-7 and 9 passed; LLM/publish skipped by diagnostic flags
- Fixed two issues found by the run: pending SEC names were mislabeled as price-needed, and the generated review list needed a JSON handoff for later SEC coverage gates.

## Communication preference update

- User requested simple, understandable English going forward.
- Avoid technical jargon unless asked.
- Define any needed internal term in plain language before using it.
- Added this rule to `memory/MEMORY.md` and the active fundamental history replay plan.

## QQQ daily CLI

- Built `aeternus qqq-daily --format table|json`.
- Service: `tradingagents/phase_engine/qqq_daily.py`.
- Output combines:
  - V3 QQQ RTH/overnight long signals
  - QQQ-gated SPY S7 hedge state
  - CCWyckoff QQQ CC-day bucket: S2 `rth_avoid`, S3 `markup_fade`, S4 `markdown_crush`, S7b `weak_regime_rth`
  - V3 return buckets: no suppression, S7 suppression, QQQ buy-hold
  - CCWyckoff return buckets by signal and total
- As-of contract: default latest common completed QQQ/SPY bar; `--as-of YYYY-MM-DD` truncates both signals and historical return buckets to that selected common bar.
- Oracle QAQC initially blocked on as-of leakage and date alignment; fixed by sorting/asserting QQQ/SPY aligned dates and truncating returns to selected as-of. Final Oracle status: Approved.
- Verification: `tests/test_qqq_daily.py tests/test_cli_momentum_scan.py tests/test_phase3_modules.py::TestIndexOverlayEngine tests/test_hedging.py tests/test_market_regime.py -q` -> `42 passed, 2 warnings`; py_compile passed.
- Live smoke as of `2026-05-12`: RTH `NO_V3_RTH_LONG`, overnight `LONG_QQQ`, hedge `NO_S7_HEDGE`, covered call `NO_CCWYCKOFF_CC_DAY`; V3 with suppression `+961.28 pts` / `+1882.56%`; CCWyckoff all `1399` trades / `+174.81%` / `+284.96 pts`.
