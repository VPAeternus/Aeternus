# Session Journal — 2026-03-10

## Manual X-Feed Start

- Objective: start today's manual X-feed workflow for `2026-03-10`.
- Ran readiness check:
  - `python3 -m cli.main x-feed --status --date 2026-03-10`
  - result: `INCOMPLETE`
  - completed passes: `0/15`
  - merged symbols: `0`
- Generated today's manual prompt artifact:
  - `python3 -m cli.main x-feed --generate --date 2026-03-10 > eval_results/x_feed/2026-03-10/manual_prompts.txt`
  - verified artifact path: `eval_results/x_feed/2026-03-10/manual_prompts.txt`
  - verified size: `1041` lines
- Root cause of first generation failure:
  - directory creation and output redirection were launched in parallel
  - redirection failed before the date directory existed
- Resolution:
  - reran sequentially after confirming `eval_results/x_feed/2026-03-10/` existed
  - artifact generated successfully
- Current state:
  - manual X-feed for `2026-03-10` is ready for operator use
  - ingestion has not started yet; workflow remains blocked until passes are ingested

## Manual X-Feed Completion

- Ingested all manual X-feed passes for `2026-03-10`:
  - passes `1-14` wrote ticker entries into the merged X-feed artifact
  - pass `15` used a `gex_regime` payload and correctly wrote as a GEX-only pass
- Final readiness check:
  - `python3 -m cli.main x-feed --status --date 2026-03-10`
  - result: `READY`
  - completed passes: `15/15`
  - merged symbols: `70`
- Final key artifacts:
  - `eval_results/x_feed/2026-03-10/merged.json`
  - `eval_results/x_feed/2026-03-10/raw/pass_01.json` ... `pass_15.json`
- Quality note:
  - a prior candidate for pass `15` was rejected because it returned stock-level `trending` names instead of the required `gex_regime` schema
  - accepted pass `15` is a neutral no-data fallback, which is preferable to fabricated GEX content

## Discover Recall Cleanup

- Root cause of the repeated `possibly delisted; no price data found` warnings:
  - stale symbols still existed in both `eval_results/control/knowledge_graph.json` and `tradingagents/graph/data/universe_constituents.csv`
  - the discovery FVG/FMA recall channels batch-downloaded AKG symbols but did not confirm or prune Yahoo-empty names
- Verified the warned set returned empty Yahoo frames on `2026-03-10`:
  - `GATO HONE IIVIP ITOS JNCE KRTX MRUS PPBI PRFT TBK UMPQ VBTX YMAB`
- Implemented:
  - recall-path helper in `tradingagents/dealflow/pipeline.py` that:
    - suppresses Yahoo warning noise during recall downloads
    - re-checks missing symbols individually on Yahoo
    - removes confirmed Yahoo-empty symbols from AKG via `remove_nodes(...)`
  - persistent AKG node-removal support in `tradingagents/graph/knowledge_graph.py`
  - bootstrap CSV cleanup in `tradingagents/graph/data/universe_constituents.csv`
- One-off workspace cleanup:
  - pruned the same 13 symbols from `eval_results/control/knowledge_graph.json`
- Verification:
  - `python3 -m pytest tests/test_dealflow_pipeline.py -k 'discover_persists_fvg_recall_artifact_and_threads_symbols or discover_persists_fma_recall_artifact_and_threads_symbols or prunes_symbols_with_no_yahoo_history_from_akg' -q`
    - result: `3 passed`
  - `python3 -m cli.main discover --date 2026-03-10 --format table`
    - result: `Discovery complete | Universe size: 70`
    - no repeated delisted-symbol warning burst during the run

## Universe Filter Gate Restore

- Root cause:
  - the runbook and CLI expected a first-universe-filter gate between `discover` and `collect`
  - `cli.commands.dealflow.discover()` still rendered `summary["universe_filter"]`
  - but `DealFlowPipeline.discover()` had stopped building/persisting `universe_filter.json`
- Implemented in `tradingagents/dealflow/pipeline.py`:
  - initialize `x_feed_merged` explicitly
  - build `universe_filter_report = build_universe_filter_report(...)`
  - persist `eval_results/deal_flow/YYYY-MM-DD/universe_filter.json`
  - return both `universe_filter` and `universe_filter_summary` in the discover payload
- Verification:
  - `python3 -m pytest tests/test_dealflow_pipeline.py -k 'discover_persists_universe_filter_artifact_and_summary' -q`
    - result: `1 passed`
  - `python3 -m pytest tests/test_dealflow_pipeline.py -k 'discover_persists_fvg_recall_artifact_and_threads_symbols or discover_persists_fma_recall_artifact_and_threads_symbols or prunes_symbols_with_no_yahoo_history_from_akg or discover_persists_universe_filter_artifact_and_summary' -q`
    - result: `4 passed`
  - `python3 -m cli.main discover --date 2026-03-10 --format table`
    - result: CLI now shows `First Universe Filter`
  - `python3 -m cli.main universe-filter --date 2026-03-10 --status`
    - result: `CHECK First Universe Filter — 2026-03-10`
    - actual failing checks:
      - `technical_recall_present = 0`
      - `scout_activity_present = 0`
- Interpretation:
  - the gate is now functioning
  - it is correctly flagging thin discovery / no technical recall as a real pre-collect issue rather than hiding behind a missing artifact

## Universe Filter Debug Closure

- Root cause of the remaining `technical_recall_present = 0` failure:
  - both recall builders in `tradingagents/dealflow/pipeline.py` treated missing AKG `liquidity_score` as `50.0`
  - that made the recall candidate set depend on unrated AKG nodes and obscured real eligibility problems
- Implemented:
  - added `_coerce_liquidity_score(...)`
  - changed both FVG and FMA recall builders to skip nodes whose liquidity is missing
  - added focused regressions:
    - `test_fvg_recall_skips_symbols_without_liquidity_score`
    - `test_fma_recall_skips_symbols_without_liquidity_score`
- Focused verification:
  - `python3 -m pytest tests/test_dealflow_pipeline.py -k 'skips_symbols_without_liquidity_score or prunes_symbols_with_no_yahoo_history_from_akg or discover_persists_universe_filter_artifact_and_summary' -q`
    - result: `4 passed`
- Live finding during debug:
  - a long-running `discover` process had loaded AKG before a bounded liquidity repair and later saved its stale in-memory graph, wiping those liquidity updates
  - reran the small liquidity repair sequentially after `discover` completed
- Deterministic recovery path:
  - verified direct technical recall on the repaired AKG:
    - `FMA recall = ['NVDA']`
  - rebuilt persisted artifacts for `2026-03-10`:
    - `eval_results/deal_flow/2026-03-10/fvg_recall.json`
    - `eval_results/deal_flow/2026-03-10/fma_recall.json`
    - `eval_results/deal_flow/2026-03-10/universe_filter.json`
    - `eval_results/deal_flow/2026-03-10/discovery_delta.json`
- Final gate verification:
  - `python3 -m cli.main universe-filter --date 2026-03-10 --status`
  - result: `READY First Universe Filter — 2026-03-10`
  - passing checks:
    - `technical_recall_present = 1`
    - `manual_xfeed_present = 70`
    - `scout_activity_present = 1`
- Current safe next step:
  - `python3 -m cli.main collect --date 2026-03-10 --trigger manual --profile daily --top-k 30 --format table`

## Deep Analysis Timing Probe

- Ran the first real single-name deep analysis timing probe on the top selected queue item:
  - symbol: `RLMD`
  - queue id: `2026-03-10-082214-manual:RLMD`
  - command:
    - `/usr/bin/time -p python3 -m cli.main analyze --from-queue-id 2026-03-10-082214-manual:RLMD --run-date 2026-03-10`
- First attempt failed fast in `4.97s` before the analyst loop:
  - root cause: `FinancialSituationMemory` still tried to initialize an OpenAI embedding client for `claude_cli` local-provider runs when no embedding API keys were present
  - env check in this workspace at the time:
    - `OPENAI_API_KEY = false`
    - `ANTHROPIC_API_KEY = false`
    - `XAI_API_KEY = false`
    - `GOOGLE_API_KEY = false`
- Implemented fix in `tradingagents/agents/utils/memory.py`:
  - local CLI providers (`claude_cli`, `codex_cli`) now disable memory embeddings gracefully when no real embedding vendor is configured
  - if Google or OpenAI embeddings are configured, they are still used as fallback
- Added focused regression coverage in `tests/test_claude_cli.py`:
  - `test_financial_situation_memory_local_cli_provider_disables_embeddings_without_keys`
- Verification:
  - `python3 -m pytest tests/test_claude_cli.py -k 'financial_situation_memory_local_cli_provider_disables_embeddings_without_keys or trading_graph_claude_cli_no_api_key_required' -q`
    - result: `3 passed`
- Second RLMD timing probe after the fix:
  - startup blocker cleared
  - analysis entered the real deep path and then exceeded `60s` without finishing
  - emitted a Yahoo warning during the live run:
    - `SPY: YFRateLimitError('Too Many Requests. Rate limited. Try after a while.')`
  - no partial `results/RLMD/2026-03-10/analysis_report.json` was written before the probe was stopped
- Interpretation:
  - the current blocker is no longer local-provider startup / memory initialization
  - the remaining runtime issue is inside the real analyst/tool execution path and still needs bounded batch strategy:
    - `--selected-only`
    - shorter `--per-item-timeout-seconds`
    - optional chunking with `--max-items`

## Bounded Analyze-Batch Probe

- Ran bounded batch probe:
  - `python3 -m cli.main retired post-scout batch command --run-date 2026-03-10 --selected-only --max-items 5 --per-item-timeout-seconds 240 --format table`
- Findings:
  - `RLMD` did not complete inside the `240s` item budget
  - the batch process advanced to the next item after the item guard, but the spawned Codex analyst subprocesses for `RLMD` remained alive as orphaned children
  - `BATL` and `TPL` both failed quickly with:
    - `RuntimeError: Codex bridge incomplete for <ticker>: ... invalid artifact`
- Artifact inspection:
  - `results/BATL/2026-03-10/codex_research/{market,social,news,fundamentals}.json`
  - `results/TPL/2026-03-10/codex_research/{market,social,news,fundamentals}.json`
  - all four analyst artifacts existed for both names
  - each artifact had:
    - `status = FAILED`
    - `exit_code = 0`
    - empty `report_markdown`
  - example durations:
    - `BATL market`: `16.43s`
    - `TPL market`: `20.99s`
- Interpretation:
  - there are now two separate deep-analysis bugs:
    1. long names like `RLMD` can exceed the item budget and leave orphaned Codex child processes behind
    2. even when Codex returns quickly, the bridge is treating the produced artifacts as invalid because `report_markdown` is empty despite `exit_code = 0`
- Operational action taken:
  - stopped the bounded batch after enough evidence was collected
  - terminated the active batch parent and spawned Codex child processes for the in-flight names to avoid burning more queue items / local resources
- Next debug target:
  - inspect `tradingagents/graph/codex_research_bridge.py` and the raw Codex output files for why successful CLI exits are yielding empty `report_markdown`

## Opus46 Comparison

- Root-cause comparison against `../AeternusAgents-opus46`:
  - `cli/commands/scoring.py` is materially identical between the two workspaces
  - `tradingagents/graph/codex_research_bridge.py` is materially identical between the two workspaces
  - `tradingagents/default_config.py` is also identical for the deep-analysis provider defaults:
    - `research_analyst_provider = gpt`
    - `research_post_analyst_provider = claude`
- Evidence from `opus46` results:
  - recent successful daily artifacts for `2026-03-09` are under `results/*/2026-03-09/claude_research/`
  - no corresponding `2026-03-09/codex_research/` artifacts were found for that daily set
  - this indicates the working daily path there was Claude-analyst oriented, not GPT/Codex-analyst oriented
- Direct runtime comparison in this workspace:
  - prior failing path:
    - `python3 -m cli.main retired post-scout batch command --run-date 2026-03-10 --selected-only --max-items 5 --per-item-timeout-seconds 240 --format table`
    - `BATL` and `TPL` failed in `codex_research` with `exit_code = 0` and empty `report_markdown`
  - minimal Claude-path probe:
    - `/usr/bin/time -p python3 -m cli.main analyze --from-queue-id 2026-03-10-082214-manual:BATL --run-date 2026-03-10 --analyst-provider claude --post-analyst-provider claude`
    - produced valid `claude_research/market.raw.json`, `claude_research/market.json`, `claude_research/social.raw.json`, and `claude_research/social.json`
    - first valid parsed artifact:
      - `results/BATL/2026-03-10/claude_research/market.json`
      - `status = SUCCESS`
      - `duration_seconds = 64.35`
- Conclusion:
  - the earlier failure was not caused by a large code divergence from `opus46`
  - the practical difference was that the failing run used GPT analysts via the Codex bridge, while the known-good path in `opus46` was using Claude/session or Claude-analyst execution

## Session Engine Flip

- Replaced the default deep-analysis boundary with a top-level session-style research engine:
  - new module: `tradingagents/graph/session_research_engine.py`
  - behavior:
    - gather one computation packet via `session_assembler.gather_computation_data(...)`
    - run one local LLM completion against that packet (`claude` or `gpt`)
    - score in Python via `session_assembler.build_session_score(...)`
    - persist the downstream `analysis_report.json` via `session_assembler.write_analysis_report(...)`
- `cli/commands/scoring.py` now dispatches `run_analysis(...)` to the session engine when:
  - `research_execution_mode = session_engine`
  - `analyst_provider in {claude, gpt}`
- Flipped defaults in `tradingagents/default_config.py`:
  - `research_execution_mode = session_engine`
  - `research_analyst_provider = claude`
  - `research_post_analyst_provider = claude`
- The legacy provider-aware bridge path remains available by setting:
  - `AETERNUS_RESEARCH_EXECUTION_MODE=codex_bridge`

- Focused verification:
  - `python3 -m pytest tests/test_session_research_engine.py tests/test_scoring_codex_bridge.py -q`
    - result: `9 passed`
  - `python3 -m pytest tests/test_cli_dealflow.py -k 'analyze_batch_selected_only_runs_marked_items or analyze_batch_default_uses_quick_mode_for_unselected or analyze_batch_uses_cached_report_when_execution_fails' -q`
    - result: `3 passed`

- Live smoke 1:
  - command:
    - `/usr/bin/time -p python3 -m cli.main analyze --from-queue-id 2026-03-10-082214-manual:BATL --run-date 2026-03-10`
  - result:
    - completed successfully in `81.02s`
    - wrote `results/BATL/2026-03-10/analysis_report.json`
    - `final_trade_decision` is present and batch-compatible
    - `aeternus_score = 55.41`, `rating = Hold`, `confidence = 5`

- Live smoke 2:
  - command:
    - `python3 -m cli.main retired post-scout batch command --run-date 2026-03-10 --selected-only --max-items 1 --per-item-timeout-seconds 240 --format json`
  - result:
    - `research_execution_mode = session_engine`
    - processed `1` item (`RLMD`)
    - `success_count = 1`
    - wrote `results/RLMD/2026-03-10/analysis_report.json`
    - wrote `eval_results/deal_flow/2026-03-10/retired_batch_summary_101611.json`

- Hardened performance-monitoring capture for the new session engine:
  - `cli/commands/scoring.py`
    - session-engine analysis now appends to `eval_results/track_record.json`
    - both analysis paths now persist explicit `recommendation` alongside the Python `rating`
  - `cli/common.py`
    - extracted analysis outcomes now include `rating_id` and `recommendation_side`
  - `tradingagents/evidence/live_outcomes.py`
    - predicted direction now prefers `recommendation` over `rating`
- Focused verification:
  - `python3 -m pytest tests/test_scoring_codex_bridge.py tests/test_session_research_engine.py tests/test_live_outcomes.py -q`
    - result: `11 passed`
  - `python3 -m pytest tests/test_cli_dealflow.py -k 'extract_analysis_outcome_exposes_rating_id_and_recommendation_side or analyze_batch_selected_only_runs_marked_items or analyze_batch_default_uses_quick_mode_for_unselected or analyze_batch_uses_cached_report_when_execution_fails' -q`
    - result: `4 passed`
- Live monitoring smoke:
  - command:
    - `python3 -m cli.main analyze --from-queue-id 2026-03-10-082214-manual:OXY --run-date 2026-03-10`
  - result:
    - appended first session-engine row to `eval_results/track_record.json`
    - persisted:
      - `rating_id = b590a5f0-a5d4-4f15-9cfe-17f893efaeb7`
      - `aeternus_score = 58.2`
      - `rating = Hold`
      - `recommendation = BUY`

- Recorded manual Robinhood benchmark fill:
  - command:
    - `python3 -m cli.main add-position QQQ 100 607.78 --date 2026-03-10 --lane MOMENTUM`
  - result:
    - persisted `QQQ` in `eval_results/paper_execution/positions.json`
    - persisted AKG current position for `QQQ`
    - stored:
      - `net_quantity = 100`
      - `avg_price = 607.78`
      - `market_value_usd = 60778.0`
      - `lane = MOMENTUM`
  - note:
    - benchmark overlay entry has no `rating_id`, so `rating_ids = []` and `entry_aeternus_score = 0.0`

- Added explicit V3 hurdle-rejected tracking:
  - `tradingagents/dealflow/cohort_tracker.py`
    - new cohort: `V3_REJECTED`
    - new metric: `benchmark_to_v3_rejected`
  - `cli/commands/cohort_compare.py`
    - now renders `V3_REJECTED`
    - now renders `QQQ→V3 Rejected` / benchmark-vs-rejected decision alpha
- Verification:
  - `python3 -m pytest tests/test_cohort_tracker.py tests/test_cohort_compare.py -q`
    - result: `2 passed`
  - live:
    - `python3 -m cli.main cohort-compare --date 2026-03-10`
    - same-day result:
      - `ENTERED = 31`
      - `SELECTED = 13`
      - `V3_CLEARED = 0`
      - `V3_REJECTED = 13`
      - `QQQ→V3 Rejected = +0.0%`
    - interpretation:
      - same-day horizon has no forward information yet; rerun at later eval dates for real signal

- Added a recent-run `why-missed` audit loop:
  - new module:
    - `tradingagents/dealflow/why_missed.py`
  - new CLI:
    - `python3 -m cli.main why-missed <TICKER>`
  - hard cap:
    - always inspects at most the last `5` dated runs, even if a larger lookback is requested
  - artifacts inspected per run:
    - `eval_results/x_feed/<date>/merged.json`
    - `eval_results/deal_flow/<date>/signals_raw.json`
    - `eval_results/deal_flow/<date>/legacy_scored_artifact.json`
    - `eval_results/deal_flow/<date>/legacy_queue_artifact.json`
    - `eval_results/deal_flow/<date>/retired_batch_latest.json`
    - latest dated `eval_results/paper_execution/plans/<date>/portfolio_plan_*.json`
  - current root-cause labels:
    - `NOT_FLAGGED`
    - `DISCOVERY_CUT`
    - `SIGNAL_FILTER_CUT`
    - `CANDIDATE_LIST_CUT`
    - `DEEP_SELECTION_CUT`
    - `ANALYSIS_NOT_COMPLETED`
    - `V3_HURDLE_REJECTED`
    - `PORTFOLIO_CONSTRUCTION_CUT`
    - `DEPLOYED`
  - focused verification:
    - `python3 -m pytest tests/test_why_missed.py tests/test_cli_dealflow.py -k 'why_missed' -q`
      - result: `2 passed`
  - live smoke:
    - `python3 -m cli.main why-missed MU --format json`
      - `MU` flagged in `2/5` recent runs, both ending at `CANDIDATE_LIST_CUT`
    - `python3 -m cli.main why-missed BE --format json`
      - `BE` flagged in `1/5` recent runs, ending at `DISCOVERY_CUT` on `2026-03-06`

- Extended `why-missed` with stage-drop and forward-return enrichment:
  - each run now includes:
    - `stage_drops`
    - `primary_stage_drop`
    - `improvement_target`
    - `forward_eval_date`
    - `forward_status`
    - `return_5d`
    - `benchmark_return_5d`
    - `edge_vs_benchmark_5d`
    - `review_recommended`
    - `review_reason`
  - stage-drop source:
    - `hypothesis_ledger/*/rows.json` plus dropped snapshot files for the specific cycle
  - forward-return behavior:
    - only computes if the cycle is old enough to have a T+5 close
    - otherwise marks the run as `PENDING`
  - review trigger:
    - emits a recommended review when a missed run has a valid stage target and `edge_vs_benchmark_5d >= +1.00%`
  - CLI changes:
    - `why-missed` table now shows `Target` and `5d Edge`
    - per-run target lines and review warnings render below the table
  - focused verification:
    - `python3 -m pytest tests/test_why_missed.py tests/test_cli_dealflow.py -k 'why_missed' -q`
      - result: `3 passed`
  - live smoke:
    - `python3 -m cli.main why-missed MU --format json`
      - live `2026-03-10` row now shows:
        - `primary_stage_drop = candidate_list_cut`
        - `improvement_target = candidate_list_cut`
        - live candidate_list ledger `rule_snapshot`
      - all forward-return fields are currently `PENDING` because the recent 5 runs have not reached T+5 yet

- Fixed the manual X-feed carry-forward gap exposed by `BE`:
  - actual root cause:
    - `BE` existed in `eval_results/x_feed/2026-03-06/merged.json`
    - there was no `eval_results/deal_flow/2026-03-06/` cycle directory
    - so the name was never evaluated downstream after X-feed ingestion
  - implemented `load_recent_merged(...)` in `tradingagents/dealflow/sources/x_feed_manual.py`
    - same-day X-feed always included
    - recent prior X-feed dates included only if their same-date dealflow directory is absent
    - carried names annotated with:
      - `x_feed_source_date`
      - `x_feed_carryforward_days`
  - wired `tradingagents/dealflow/pipeline.py` discovery and standalone collect bootstrap to use the new helper
  - added config:
    - `dealflow_manual_x_feed_carryforward_days`
    - default `3`
    - env `DEALFLOW_MANUAL_X_FEED_CARRYFORWARD_DAYS`
  - focused verification:
    - `python3 -m pytest tests/test_x_feed_manual.py tests/test_dealflow_pipeline.py -k 'load_recent_merged or carries_forward_recent_unprocessed_x_feed_symbols or persists_universe_filter_artifact_and_summary' -q`
      - result: `4 passed`
  - live smoke:
    - `load_recent_merged('2026-03-09', lookback_days=3)` now returns `BE` from `2026-03-06`

- Corrected `why-missed` to classify pipeline completeness gaps separately:
  - new root cause:
    - `PIPELINE_NOT_RUN`
  - new improvement target:
    - `daily_run_completeness`
  - semantics:
    - used when a ticker appears in X-feed for a source date but there is no same-date `deal_flow/YYYY-MM-DD` cycle directory
  - focused verification:
    - `python3 -m pytest tests/test_why_missed.py tests/test_cli_dealflow.py -k 'why_missed' -q`
      - result: `4 passed`
  - live smoke:
    - `python3 -m cli.main why-missed BE --format json`
      - `2026-03-06` now reports:
        - `root_cause = PIPELINE_NOT_RUN`
        - `improvement_target = daily_run_completeness`

- Added a `needle-retro` false-negative review surface:
  - new module:
    - `tradingagents/dealflow/needle_retro.py`
  - new CLI:
    - `python3 -m cli.main needle-retro`
  - purpose:
    - scan up to the last `5` completed, realized cycles
    - exclude infrastructure misses such as `PIPELINE_NOT_RUN`
    - rank true false negatives by 5-day edge vs benchmark
    - summarize improvement pressure by `improvement_target`
  - implementation details:
    - uses `compute_hindsight(...)` for realized forward-return data
    - uses new `audit_ticker_run(...)` helper in `tradingagents/dealflow/why_missed.py` for per-cycle stage/root-cause classification
  - focused verification:
    - `python3 -m pytest tests/test_needle_retro.py tests/test_why_missed.py tests/test_cli_dealflow.py -k 'needle_retro or why_missed' -q`
      - result: `6 passed`
  - live smoke:
    - `python3 -m cli.main needle-retro --format json`
      - current workspace output:
        - `cycles_completed = 0`
        - `opportunities = []`
      - reason:
        - this clone currently does not have older realized hindsight-eligible cycles with the required artifacts, so the new operator surface is live but the local review dataset is empty

- Added a replay-only KAMA cross backtest surface:
  - new module:
    - `tradingagents/dealflow/kama_recall.py`
  - new CLI:
    - `python3 -m cli.main kama-backtest`
  - signal definition:
    - bullish event = `(1,10,15)` KAMA crossing above `(2,10,15)` KAMA
  - scope:
    - replay/evidence only
    - not wired into live discovery or portfolio admission
  - overlap diagnostics now compare KAMA vs current technical recall families:
    - `kama_only`
    - `fvg_only`
    - `fma_only`
    - combined overlap buckets
  - focused verification:
    - `python3 -m pytest tests/test_kama_recall.py tests/test_cli_dealflow.py -k 'kama_backtest or kama_snapshot or kama_overlap' -q`
      - result: `6 passed`
    - `python3 -m pytest tests/test_kama_recall.py tests/test_cli_dealflow.py -k 'kama or fma_backtest or fvg_backtest' -q`
      - result: `10 passed`
  - live smoke:
    - `python3 -m cli.main kama-backtest --tickers NVDA --tickers AMD --benchmark SMH --start 2024-01-01 --top-n 1 --format json`
    - result:
      - `event_count = 7`
      - `avg_edge_vs_benchmark_20d = +1.05%`
      - `avg_edge_vs_benchmark_60d = +1.09%`
    - artifact dir:
      - `eval_results/deal_flow/kama_backtest/2026-03-10/semis_ai_narrow-vs-SMH`

- Upgraded the KAMA replay rule from loose overlap to `KAMA cross inside bullish FVG regime`:
  - new helper:
    - `build_bullish_fvg_regime_frame(...)` in `tradingagents/dealflow/kama_recall.py`
  - regime contract:
    - bullish FVG increments `bullish_fvg_streak`
    - bearish FVG resets regime immediately
    - regime expires after `20` bars with no new bullish FVG
  - KAMA row gating:
    - `kama_active = cross_up and bullish_fvg_regime_active`
  - row-level inspection fields now persisted:
    - `bullish_fvg_regime_active`
    - `bullish_fvg_streak`
    - `bullish_fvg_regime_age_bars`
  - focused verification:
    - `python3 -m pytest tests/test_kama_recall.py -q`
      - result: `7 passed`
    - `python3 -m pytest tests/test_kama_recall.py tests/test_cli_dealflow.py -k 'kama or fma_backtest or fvg_backtest' -q`
      - result: `13 passed`
  - live `MU` / `BE` smoke:
    - `python3 -m cli.main kama-backtest --tickers MU --tickers BE --benchmark QQQ --start 2020-01-01 --top-n 1 --format json`
    - result:
      - `event_count = 10` under regime gating, down from `14`
      - `avg_edge_vs_benchmark_20d = -2.11%`
      - `avg_edge_vs_benchmark_60d = +13.19%`
      - `avg_edge_vs_benchmark_90d = +31.79%`
      - surviving regime-qualified events:
        - `MU = 3`
        - `BE = 7`
      - latest surviving events:
        - `MU: 2025-06-13`
        - `BE: 2025-06-16`

- Extended replay horizons to include `30d`:
  - shared summary/basket support updated in:
    - `tradingagents/dealflow/fvg_recall.py`
    - `tradingagents/dealflow/fma_recall.py`
    - `tradingagents/dealflow/kama_recall.py`
  - `kama-backtest` table output now renders `30d`
  - focused verification:
    - `python3 -m pytest tests/test_kama_recall.py tests/test_cli_dealflow.py -k 'kama' -q`
      - result: `9 passed`

- Ran a one-off exit probe on the `MU` / `BE` regime-qualified KAMA entries:
  - sample size:
    - `10` entries
  - fixed-hold edge vs `QQQ`:
    - `20d: -2.11%`
    - `30d: -1.57%`
    - `60d: +13.19%`
    - `90d: +31.79%`
  - dynamic exits (analysis script only):
    - `exit_kama_down`:
      - `avg_return = +65.0%`
      - `avg_edge_vs_benchmark = +63.9%`
      - `avg_hold_bars = 94.5`
    - `exit_bearish_fvg`:
      - `avg_return = -3.5%`
      - `avg_edge_vs_benchmark = -4.6%`
      - `avg_hold_bars = 17`
    - `exit_first_of_both` matched bearish-FVG exits on this sample
  - read:
    - short horizons remain weak on this sample
    - most of the observed edge appears in longer holds / late exits
    - not enough evidence yet to promote to live entry logic

- Expanded the replay to the live top-50 `QQQ` basket as of `2026-03-10`:
  - holdings basis:
    - ETF Channel `QQQ` holdings pages 1-3
  - artifacts:
    - `eval_results/deal_flow/kama_backtest/2026-03-10/qqq_top50_live-vs-QQQ/comparison_fixed_holds.json`
    - `eval_results/deal_flow/kama_backtest/2026-03-10/qqq_top50_live-vs-QQQ/exit_probe.json`
  - basket-level `top_n=1` edge vs `QQQ`:
    - plain `KAMA`:
      - `5d: +0.10%`
      - `20d: +1.05%`
      - `30d: +1.64%`
      - `60d: +2.84%`
      - `90d: +5.76%`
    - `KAMA inside bullish FVG regime`:
      - `5d: +0.21%`
      - `20d: +1.33%`
      - `30d: +1.42%`
      - `60d: +2.45%`
      - `90d: +5.42%`
  - read:
    - the broader basket confirms both variants have edge
    - the FVG regime gate improves shorter-horizon entry quality
    - plain `KAMA` remains slightly stronger from `30d` onward
  - exit probe on the same event set:
    - plain `KAMA`:
      - `hold_20 edge: +0.67%`
      - `hold_30 edge: +1.07%`
      - `hold_60 edge: +2.31%`
      - `hold_90 edge: +4.98%`
      - `exit_kama_down edge: +7.55%`
      - `exit_bearish_fvg edge: +1.10%`
    - `KAMA inside bullish FVG regime`:
      - `hold_20 edge: +0.93%`
      - `hold_30 edge: +0.95%`
      - `hold_60 edge: +2.10%`
      - `hold_90 edge: +4.95%`
      - `exit_kama_down edge: +9.46%`
      - `exit_bearish_fvg edge: +1.08%`

- Extended the same current-top-50 `QQQ` basket replay back to `1999-01-01`:
  - artifacts:
    - `eval_results/deal_flow/kama_backtest/2026-03-10/qqq_top50_live_1999-vs-QQQ/comparison_fixed_holds.json`
    - `eval_results/deal_flow/kama_backtest/2026-03-10/qqq_top50_live_1999-vs-QQQ/exit_probe_core.json`
  - caveat:
    - this is a current-top-50 survivor basket replayed backward, not a point-in-time constituent backtest, so the result is meaningfully survivorship-biased
  - `top_n=1` basket edge vs `QQQ`:
    - plain `KAMA`:
      - `5d: +0.28%`
      - `20d: +0.71%`
      - `30d: +1.17%`
      - `60d: +2.17%`
      - `90d: +3.25%`
    - `KAMA inside bullish FVG regime`:
      - `5d: +0.33%`
      - `20d: +0.87%`
      - `30d: +1.30%`
      - `60d: +2.38%`
      - `90d: +3.47%`
  - exit-core read:
    - plain `KAMA`:
      - `exit_kama_down edge: +12.89%`
      - `exit_bearish_fvg edge: +0.65%`
    - `KAMA inside bullish FVG regime`:
      - `exit_kama_down edge: +15.08%`
      - `exit_bearish_fvg edge: +0.64%`
  - read:
    - the long survivor-basket window reinforces the same pattern seen on the 2020+ sample
    - the regime gate modestly improves quality
    - most of the edge still comes from staying with the trend until `KAMA` rolls over

## SPY QQQ DOW Signal Cache Design

- Approved the next infrastructure slice after the KAMA/FVG replay work:
  - current `SPY + QQQ + DOW` constituents only
  - objective is broad live-signal validation and current buy-zone screening
- Saved:
  - `docs/plans/2026-03-11-spy-qqq-dow-signal-cache-design.md`
  - `docs/plans/2026-03-11-spy-qqq-dow-signal-cache.md`
- Chosen v1 architecture:
  - SQLite DB at `eval_results/control/technical_signal_cache.db`
  - tables for:
    - current universe membership
    - daily OHLCV cache
    - daily KAMA/FVG signal state
    - current buy-zone state
- Planned operator commands:
  - `technical-universe-refresh`
  - `technical-signal-sync`
  - `buy-zone`
  - `buy-zone-summary`
- Important scope note:
  - this v1 will use current constituents only and label historical results as `current-constituent replay`
  - point-in-time membership reconstruction is deferred
