# 2026-05-05

## Context-mode skill conflict fix

- Fixed Pi skill loader conflict: `/opt/homebrew/lib/node_modules/context-mode/skills/UPSTREAM-CREDITS.md` lacked required `description` metadata.
- Added YAML frontmatter with `name: upstream-credits` and a non-operational `description`.
- Verified with Python assertion that frontmatter exists and includes `description`.

## X-feed Theme Emergence Graph v1

- Upgraded manual X-feed ingestion from destructive later-pass ticker overwrite to per-ticker evidence preservation.
- Added parsed fields for `accounts_cited`, `theme_links`, `co_mentions`, `why_this_is_new`, `order_type`, `source_quality`, and `no_source_found`.
- Added `theme_emergence_graph.json` under `eval_results/x_feed/<date>/` with account→ticker, ticker→theme, and ticker→ticker edges plus theme/ticker emergence scores.
- Adjusted social/news scoring to use explicit evidence counts, theme emergence bonus, rank-aware buzz handling, and penalties for `NO_SOURCE_FOUND`.
- Kept 15-pass design for now; graph merge mitigates overlap, with recommendation to compress only after pass-yield telemetry.
- Verification: `pytest tests/test_x_feed_manual.py tests/test_social_cache_coverage.py tests/test_dealflow_social_news_live.py tests/test_x_feed_browser_runner.py -q` → 33 passed; `pytest tests/test_x_feed_options_flow.py tests/test_x_feed_gex.py tests/test_cli_x_feed_manual.py -q` → 14 passed.
