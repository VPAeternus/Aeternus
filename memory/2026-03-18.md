# 2026-03-18

- Resumed work from:
  - `docs/plans/2026-03-17-why-missed-observability-gap.md`

- Implemented stage-native "why missed" diagnostics in `question-investigate` backend:
  - rebuilt:
    - `tradingagents/dealflow/investigation_runner.py`
  - `stage_diagnosis` entries now include:
    - `reason_code`
    - `reason_text`
    - `threshold`
    - `observed_value`
    - `delta_to_pass`
    - `artifacts`

- Added hypothesis-ledger aware reject-reason support:
  - reads stage drop snapshots from:
    - `hypothesis_ledger/*/rows.json`
  - maps drop context into investigation-stage reasons for:
    - `universe_filter`
    - `collect`
    - `candidate_list`
    - `fundamental_intake`

- Added candidate_list miss precision:
  - score-based rank reconstruction from `legacy_scored_artifact`
  - reasons:
    - `RANK_BELOW_CANDIDATE_LIST_CUT`
    - `CANDIDATE_LIST_EXCLUSION_ANOMALY` (rank inside `top_k` but missing from candidate_list artifact)

- Test updates:
  - `tests/test_investigation_runner.py`
    - now validates reason metadata presence
    - now validates hypothesis-ledger derived universe-drop reason

- Verification:
  - `python3 -m pytest tests/test_investigation_runner.py tests/test_investigation_response.py tests/test_cli_question_investigate.py -q`
    - result: `11 passed`
  - smoke:
    - `python3 -m cli.main question-investigate --date 2026-03-11 --question "MU went up 10% today, why did we miss it?" --format json`
    - result: output now includes stage-level deterministic miss reasons

- Operator gateway startup reliability fix:
  - patched:
    - `scripts/run_operator_gateway.py`
  - adds `app_dir` + `reload_dirs` to avoid reload subprocess `ModuleNotFoundError: tradingagents`
  - verification:
    - `python3 scripts/run_operator_gateway.py --host 127.0.0.1 --port 8011 --reload`
    - server startup completed successfully in reload mode

- Implemented authoritative write-time stage-drop reject metadata for `why-missed` / `question-investigate`:
  - `tradingagents/dealflow/hypothesis_ledger.py`
    - `make_ledger_row(...)` now accepts `drop_metadata_by_symbol`
    - writes stage metadata artifact and persists `dropped_symbols_metadata_path`
  - `tradingagents/dealflow/pipeline.py`
    - stage-write metadata now emitted for:
      - `universe_gate_edge`
      - `universe_gate_haystack`
      - `evidence_gate`
      - `candidate_list_cut`
      - `fundamental_intake_cut`
  - `tradingagents/dealflow/investigation_runner.py`
    - now reads stage `dropped_symbols_metadata_path` and prefers canonical drop metadata over inferred fallback reasons
  - test updates:
    - `tests/test_hypothesis_ledger.py`
    - `tests/test_dealflow_hypothesis_ledger.py`
    - `tests/test_investigation_runner.py`
  - verification:
    - `python3 -m pytest tests/test_hypothesis_ledger.py tests/test_dealflow_hypothesis_ledger.py tests/test_investigation_runner.py tests/test_investigation_response.py tests/test_cli_question_investigate.py -q`
      - result: `19 passed`

- Fixed manual `x-feed` pass-14 behavior for empty/flow-only payloads:
  - root cause:
    - pass-14 payloads with `trending: []` and populated `options_flow` were returning early and not ingesting tickers
    - empty-pass summary returned `tickers_merged: 0` even when merged symbols already existed
  - patched:
    - `tradingagents/dealflow/sources/x_feed_manual.py`
      - pre-extract pass-14 `options_flow` before early return
      - synthesize ticker entries from `options_flow` when no `trending` entries exist
      - report true existing merged count for empty-pass and gex-only summary paths
  - tests added:
    - `tests/test_x_feed_manual.py`
      - `test_ingest_pass14_options_flow_only_still_ingests_tickers`
      - `test_ingest_empty_pass_reports_existing_merged_total`
      - `test_ingest_gex_only_pass_reports_existing_merged_total`
  - verification:
    - `python3 -m pytest tests/test_x_feed_manual.py -q`
      - result: `15 passed`
    - `python3 -m cli.main x-feed --status --date 2026-03-18`
      - result: `READY`, `15/15`, merged symbols `43`

- Refactored session research scoring flow for explicit pre/post LLM influence tracking:
  - patched:
    - `tradingagents/graph/session_research_engine.py`
      - now computes `base_score` pre-LLM via `build_session_score(computation_data, {})`
      - normalizes LLM outputs against base score fallback recommendation
      - recomputes final post-LLM score and persists `llm_influence` diagnostics in `aeternus_score`
      - diagnostics include score delta, confidence delta, debate component before/after, and report completeness counts
  - tests:
    - `tests/test_session_research_engine.py`
      - added `test_run_session_research_persists_llm_influence_block`
  - verification:
    - `python3 -m pytest tests/test_session_research_engine.py tests/test_session_assembler.py -q`
      - result: `22 passed`

- Built finalized Grok upload packet with auto-filled data context (no operator edits required):
  - updated:
    - `eval_results/deal_flow/2026-03-18/grok_committee_input.json`
  - filled from real artifacts:
    - `x_feed_context` from `eval_results/x_feed/2026-03-18/merged.json` + `raw/pass_*.json`
    - `portfolio_context` from `eval_results/paper_execution/positions.json` + `latest_plan.json`
  - coverage included:
    - target basket `OXY`, `VRT`, `CF`, `CVX`, `RTX`
    - per-ticker high-signal catalysts and theme context

- Added markdown-only upload artifact for Grok environments that reject JSON uploads:
  - created:
    - `docs/prompts/grok/grok_committee_packet_2026-03-18.md`
  - includes both:
    - paste-ready committee prompt
    - embedded canonical JSON packet (same values as `grok_committee_input.json`)

- Implemented v2 Grok committee packet generation to improve decision quality:
  - new script:
    - `scripts/build_grok_committee_packet.py`
  - generated artifacts:
    - `eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json`
    - `docs/prompts/grok/grok_committee_packet_2026-03-18_v2.md`
  - v2 changes:
    - injects inline per-ticker analysis evidence from `analysis_report.json` (decision/plan/trader excerpts + key context metrics)
    - adds `data_freshness` with explicit portfolio staleness age
    - adds `decision_policy` with near-boundary handling to reduce brittle threshold-only decisions
    - upgrades prompt contract to require entry/invalidation/review triggers in model output
  - verification:
    - `python3 scripts/build_grok_committee_packet.py --date 2026-03-18 --tickers OXY,VRT,CF,CVX,RTX --allow-new-positions`
      - wrote v2 json+markdown artifacts
    - `python3 -m py_compile scripts/build_grok_committee_packet.py`
      - pass

- Hardened v2 prompt/packet contract for execution safety:
  - updated:
    - `scripts/build_grok_committee_packet.py`
  - contract upgrades:
    - required output now includes top-level `execution_gate`
    - numeric trigger fields required for each ticker decision
    - hedge action now allows `DEFER_UNTIL_FRESH_CONTEXT`
    - explicit hard rule: no OPEN/INCREASE when packet execution readiness is blocked
  - packet upgrades:
    - emits `execution_readiness` status + reasons
    - blocks new-trade readiness when portfolio snapshot is stale by policy threshold (`>=2` days)
    - persists execution policy knobs in `decision_policy.execution_policy`
  - regenerated artifacts:
    - `eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json`
    - `docs/prompts/grok/grok_committee_packet_2026-03-18_v2.md`
  - verification:
    - `python3 -m py_compile scripts/build_grok_committee_packet.py`
      - pass
    - `python3 scripts/build_grok_committee_packet.py --date 2026-03-18 --tickers OXY,VRT,CF,CVX,RTX --allow-new-positions`
      - pass

- Added search-aware committee contract and a hard output validator:
  - updated:
    - `scripts/build_grok_committee_packet.py`
      - new `search_policy` packet block
      - prompt now requires `external_enrichment` with citation + conflict log
      - external enrichment can add facts but cannot silently override packet values
  - added:
    - `scripts/validate_grok_committee_output.py`
      - validates schema completeness (`execution_gate`, per-ticker fields, portfolio actions)
      - validates stale-context policy (blocked gate cannot emit OPEN/INCREASE)
      - validates numeric trigger arrays and operator/value format
      - validates action semantics (`HOLD` only for held symbols)
      - validates external citation block when search is enabled
  - regenerated:
    - `eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json`
    - `docs/prompts/grok/grok_committee_packet_2026-03-18_v2.md`
  - verification:
    - `python3 -m py_compile scripts/build_grok_committee_packet.py scripts/validate_grok_committee_output.py`
      - pass
    - `python3 scripts/build_grok_committee_packet.py --date 2026-03-18 --tickers OXY,VRT,CF,CVX,RTX --allow-new-positions --allow-external-search`
      - pass
    - validator smoke (bad payload expected to fail):
      - `python3 scripts/validate_grok_committee_output.py --packet eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json --output /tmp/grok_bad.json`
      - result: `ok=false` with expected schema/policy errors

- Added stdin mode to validator for direct paste workflows:
  - updated:
    - `scripts/validate_grok_committee_output.py`
  - new behavior:
    - `--output -` reads model JSON from stdin
  - verification:
    - `printf '{"as_of_date":"2026-03-18","input_tickers":["CF"],"per_ticker_decisions":[]}' | python3 scripts/validate_grok_committee_output.py --packet eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json --output -`
      - result: expected fail with schema errors

- Generated a corrected, fully compliant sample committee output:
  - created:
    - `eval_results/deal_flow/2026-03-18/grok_output_corrected.json`
  - adjustments vs failing sample:
    - replaced invalid non-held `HOLD` actions with `WATCHLIST`
    - retained blocked execution gate semantics (no OPEN/INCREASE changes)
    - kept required numeric trigger arrays and external enrichment fields
    - removed citation-recency warning by updating stale citation date in sample payload
  - verification:
    - `python3 scripts/validate_grok_committee_output.py --packet eval_results/deal_flow/2026-03-18/grok_committee_input_v2.json --output eval_results/deal_flow/2026-03-18/grok_output_corrected.json`
      - result: `ok=true`, no errors/warnings

- (2026-03-19 carryover) Added prior-date report fallback support in committee packet builder:
  - updated:
    - `scripts/build_grok_committee_packet.py`
  - behavior:
    - for each ticker, resolves `analysis_report.json` at as-of date, else latest prior date
    - records provenance in packet (`aeternus_packet.report_provenance`)
    - adds explicit `known_gaps` note when fallback reports are used
  - outcome on `2026-03-19` packet:
    - `execution_readiness.status` moved from `BLOCKED_OTHER` to `PASS` with transparent fallback notes
