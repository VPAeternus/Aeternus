# 2026-05-04

## Architecture baseline review

- Reviewed committed baseline at HEAD `00e7724` (`Initial clone from AeternusAgents-codex`).
- Git status was clean at review start.
- Mapped top-level structure, package metadata, entrypoints, module/file-size hotspots, and internal import direction.
- Key findings: package metadata drift (`pyproject.toml` vs `setup.py`), README still heavily upstream TradingAgents branded, oversized god modules in CLI/graph/dealflow, `tradingagents.operator_gateway.service` imports CLI command modules, pytest not installed in active `python3` despite large test suite.

## Fundamental migration planning

- User provided source path: `/Users/aeternusholdings/Documents/AeternusAgents/fundamental`.
- Created plan: `docs/plans/2026-05-04-fundamental-migration.md`.
- Migration principle: preserve old output contracts first, no wholesale replacement.
- Current contracts identified:
  - `build_fundamental_snapshot()` top-level keys: `ratios`, `balance`, `cashflow`, `income`, `piotroski`, `data_coverage`, `fiscal_period`.
  - dealflow fundamental shadow signal fields: `symbol`, `signal_family`, `raw_score`, `source_status`, `freshness_hours`, `source_name`.
- Main compatibility risks: source requires Python `>=3.14`, newer pandas/yfinance, external cache roots, and contains monitoring/forward-return fields that must not leak into entry scoring.

## Fundamental and macro framework deletion

- Deleted current fundamental framework files from `tradingagents/agents`, `tradingagents/research/fundamental_autoresearch`, `tradingagents/dealflow/sources/fundamental_factor.py`, `tradingagents/dealflow/fundamental_shadow.py`, `cli/commands/fundamental_research.py`, old autoresearch scripts, related tests, and related plan docs.
- Deleted current macro framework files from `tradingagents/agents`, `tradingagents/backtesting/macro`, `tradingagents/dealflow/sources/macro.py`, `cli/commands/macro_prompt.py`, macro backtest scripts, related tests, and macro research docs.
- Removed direct imports/references from `tradingagents/agents/__init__.py`, `tradingagents/graph/setup.py`, `tradingagents/graph/trading_graph.py`, `tradingagents/dealflow/pipeline.py`, `tradingagents/dealflow/sources/__init__.py`, `tradingagents/dataflows/interface.py`, `tradingagents/dataflows/alpha_vantage.py`, `cli/main.py`, `cli/commands/dealflow.py`, `cli/commands/scoring.py`, `tradingagents/graph/session_assembler.py`, `tradingagents/graph/aeternus_scoring.py`, `tradingagents/graph/research_collectors.py`, `tradingagents/graph/structural_forces.py`, and `tradingagents/operator_gateway/service.py`.
- Verification: `python3 -m py_compile ...` on touched Python files passed. Runtime imports still blocked by missing environment packages (`websockets`, `langchain_core`, `langchain_openai`).

## Fundamental dealflow adapter

- Copied `fundamental` into `tradingagents/research/fundamental/` excluding `.venv`, `__pycache__`, and `*.pyc`.
- Added reusable SEC CIK resolver at `tradingagents/research/fundamental/src/ingest/cik.py`.
- Added dealflow queue adapter at `tradingagents/research/fundamental/src/pipeline/dealflow_adapter.py`.
- Adapter converts `eval_results/deal_flow/<date>/legacy_queue_artifact.json` to a fundamental universe CSV with ticker, CIK, quarter, dealflow rank/score, lane, source, why-now, tags, subscores, and evidence JSON.
- Verification: `python3 -m py_compile tradingagents/research/fundamental/src/ingest/cik.py tradingagents/research/fundamental/src/pipeline/dealflow_adapter.py` passed; monkeypatched adapter smoke test passed.

## Fundamental smoke test

- Created smoke `legacy_queue_artifact.json` for AAPL/MSFT under `/tmp/aeternus_fundamental_smoke/`.
- Ran dealflow adapter successfully: wrote `/tmp/aeternus_fundamental_smoke/dealflow_universe.csv`, 2 rows, both CIKs resolved (`AAPL=320193`, `MSFT=789019`).
- Verified direct SEC companyfacts fetch through `SecClient.companyfacts("320193")`: returned Apple facts payload with `facts=True`.
- Ran fundamental quarter pipeline with source venv and `--skip-sec-fetch --skip-llm --skip-price-fetch`; output wrote universe/pre_llm/candidate_scores/investment_decisions/research_memos parquet tables. Scores were `not_scored` / 0 because SEC and price fetch were intentionally skipped.
- Attempted full SEC-fetch quarter pipeline for AAPL/MSFT without `--skip-sec-fetch`; timed out after 180s before writing outputs. Likely too slow because it scans/fetches archive indexes across many historical filings for mega-cap tickers. Need bounded SEC fetch mode or latest-filing-only smoke before full integration.

## Fundamental SEC fetch bounded fix

- Updated `tradingagents/research/fundamental/src/pipeline/run_quarter.py` to stop fetching archive indexes for every recent SEC accession.
- It now uses existing `filings.py` helpers (`recent_filings`, `quarter_bounds`, `parse_date`) to identify the first in-quarter Item 2.02 8-K, then fetches only that 8-K archive index for exhibit discovery. Required documents remain fetched through existing `fetch_required_documents()`.
- Full smoke run for AAPL/MSFT with SEC fetch enabled and `--skip-llm --skip-price-fetch` completed within timeout. Filing events were `ready_for_fetch`; required docs fetched: `primary_8k`, `earnings_exhibit`, `periodic_10q_10k`; pre-LLM scores: AAPL 8 strong, MSFT 7 strong.

## Fundamental CLI wrapper

- Added `tradingagents/research/fundamental/src/cli/commands.py` registering `aeternus fundamental`.
- Command reads `eval_results/deal_flow/<date>/legacy_queue_artifact.json` or `--queue`, writes universe CSV under `eval_results/fundamental/<date>/` or `--output-root`, runs `run_quarter_pipeline()`, and prints `candidate_scores` table.
- Registered command in `cli/main.py`.
- Verification: `python3 -m py_compile tradingagents/research/fundamental/src/cli/commands.py cli/main.py` passed. Runtime CLI smoke blocked by environment mismatch: system Python lacks `websockets`; source framework venv lacks `typer`.

## AeternusAutoResearch fundamental improvements sync

- Reviewed `/Users/aeternusholdings/Documents/AeternusAutoResearch` against `tradingagents/research/fundamental`.
- Synced improved `src/` modules and top-level `Growth/*.py` / `Growth/*.md` artifacts into `tradingagents/research/fundamental/`, excluding caches/generated outputs.
- Preserved local Aeternus integration additions: `src/ingest/cik.py`, `src/pipeline/dealflow_adapter.py`, renamed package path, and bounded SEC archive-index fetch in `run_quarter.py`.
- Key imported improvements: repricing momentum extension, theme taxonomy/scoring configs, adaptive theme tailwind score, RM buy review path/risk controls, expanded underwriting outputs, storage/table support for theme taxonomy, updated reporting/monitoring/post-LLM scoring.
- Added `PyYAML>=6.0.2` to `tradingagents/research/fundamental/pyproject.toml` for theme config loading.
- Updated `tradingagents/research/fundamental/src/cli/commands.py` to add framework root to `sys.path` before importing framework modules that use `src.*` imports.
- Verification: py_compile over fundamental `src/*.py` plus CLI command passed; AAPL/MSFT SEC-enabled smoke with `--skip-llm --skip-price-fetch` passed and produced candidate scores with new RM/theme/underwriting columns.
