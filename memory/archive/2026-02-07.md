# Daily Log - 2026-02-07

## Session Summary

- **Timestamp**: 2026-02-07T19:55:00
- **Focus**: Step 1 hardening for Deal Flow robustness before Step 2 expansion.

## Incremental Update

- **Timestamp**: 2026-02-07T23:35:32Z
- **Focus**: Finalize 4-step P0 hardening pass (lifecycle, hedge propagation, workflow wrapper, sector cleanup).

### Completed

1. Reconciliation lifecycle hardening:
   - canonical `PARTIALLY_FILLED`
   - terminal `REPLACED`
   - aligned terminal handling in both graph reconciliation and CLI stale-order flow.

2. Workflow hedge propagation:
   - `workflow-run` now has explicit `--include-hedges/--skip-hedges`.
   - plan-step workflow telemetry now captures hedge status/action/instrument.

3. Workflow automation wrapper:
   - new `workflow-loop` command for repeated cycles with interval and failure controls.
   - persisted loop artifacts under `eval_results/live_execution/workflow/<date>/`.
   - new config defaults:
     - `workflow_loop_default_cycles`
     - `workflow_loop_interval_seconds`

4. Sector unknown cleanup:
   - manual synthesized candidates now infer sector/asset class from deterministic baseline maps.
   - unknown sector fallback normalized to `Unclassified Equity` in queue canonical map.

5. New test coverage:
   - `tests/test_paper_execution.py`:
     - partial-status normalization
     - replaced-terminal behavior
   - `tests/test_cli_dealflow.py`:
     - workflow-loop cycle and stop-on-failure behavior
   - `tests/test_dealflow_pipeline_manual.py`:
     - manual synth sector inference/fallback

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py -k "partial_status or replaced_as_terminal or applies_partial_fill_delta_only" -q` -> `3 passed`
- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -k "workflow_run_success_chains_all_steps or workflow_run_partial_analyze_failure_continues_with_warning_status or workflow_loop_runs_requested_cycles or workflow_loop_stops_on_failure_when_enabled" -q` -> `4 passed`
- `./.venv/bin/python -m pytest tests/test_dealflow_pipeline_manual.py -q` -> `2 passed`
- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py tests/test_dealflow_pipeline_manual.py -q` -> `93 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T16:12:49Z
- **Focus**: Portfolio-risk visibility to PM + robust adaptive hedging hardening.

### Completed

1. Added pre-trade risk context injection before `analyze` graph execution:
   - `build_pretrade_risk_context()` in `cli/main.py` computes non-persistent:
     - portfolio snapshot
     - market regime snapshot
     - hedge signal/decision
   - Injected into initial graph state so Risk Judge/Portfolio Manager can reason with explicit risk inputs.

2. Enhanced Risk Manager prompt (`tradingagents/agents/managers/risk_manager.py`):
   - now includes portfolio risk + hedge context block.
   - now requires explicit hedge stance in final recommendation.

3. Added dedicated risk/hedge report output in `cli/main.py`:
   - new report section: `portfolio_risk_hedge`
   - new formatter: `format_portfolio_risk_hedge_markdown(...)`
   - displayed in final CLI report and included in equity markdown report.

4. Hardened hedging policy across market conditions (`tradingagents/graph/hedging.py`):
   - deterministic regime classes: `CRASH`, `BEAR_STRESS`, `BEAR`, `VOLATILITY_SHOCK`, `HIGH_VOLATILITY`, `RISK_OFF`, `EUPHORIA`, `NEUTRAL`.
   - additional defensive floors for high-volatility and drawdown/VAR stress conditions.
   - richer `HedgeSignal` payload with `market_regime` and `risk_metrics`.
   - improved portfolio risk snapshot source using `eval_results/paper_execution/positions.json` when available.

5. Added tests:
   - `tests/test_hedging.py` (new regime-floor tests)
   - `tests/test_cli_hedging.py` (pre-trade context + formatter tests)

### Validation

- `./.venv/bin/python -m pytest tests/test_hedging.py tests/test_cli_hedging.py -v` -> `17 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `162 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T16:25:32Z
- **Focus**: Step 2 hedge-execution integration (plan -> risk check -> execution/audit).

### Completed

1. Added hedge intent builder in `tradingagents/graph/paper_execution.py`:
   - `build_hedge_order_intent(...)` converts deterministic hedge decisions into broker-compatible intents.
   - adds `intent_category=HEDGE` and hedge telemetry fields on the order payload.

2. Integrated hedge creation in `portfolio-plan` (`cli/main.py`):
   - new flag `--include-hedges/--skip-hedges` (default from config: enabled).
   - computes risk snapshot + market regime + hedge decision and appends hedge intent when `status=EXECUTED`.
   - includes `hedge_context` in persisted plan and renders hedge summary in CLI output.

3. Extended pre-trade risk guards for hedges:
   - `evaluate_pretrade_risk(...)` now accepts:
     - `max_hedge_notional_pct`
     - `allow_hedge_short_orders`
   - hedge shorts are exempt from generic short block when enabled.
   - hedge notional gets a dedicated cap independent from alpha gross/single-position caps.

4. Propagated hedge metadata through paper/live/alpaca execution records:
   - `intent_category` + hedge fields now persist to outbox/fills paths.
   - added hedge-specific audit events for intent creation/submission/execution/rejection.

5. Config updates (`tradingagents/default_config.py`):
   - `portfolio_include_hedges`
   - `execution_allow_hedge_short_orders`
   - `execution_max_hedge_notional_pct`

6. Test updates:
   - `tests/test_paper_execution.py`:
     - hedge intent generation
     - hedge short acceptance
     - hedge-cap rejection
   - `tests/test_cli_dealflow.py`:
     - portfolio-plan hedge integration
     - non-network deterministic `--skip-hedges` coverage

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `25 passed`
- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `51 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `166 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T16:30:39Z
- **Focus**: Close loop between hedge execution and persisted hedge state.

### Completed

1. Updated `execute-paper` in `cli/main.py`:
   - after accepted/submitted hedge intents are processed, execution now writes hedge state/order via `AdaptiveHedgeEngine.persist_state_and_orders(...)` using plan `hedge_context`.
   - result payload now includes `hedge_sync` summary (`state_updated`, `orders_seen`, `recorded_order`).

2. Added regression coverage:
   - `tests/test_cli_dealflow.py::test_execute_paper_updates_hedge_state_when_hedge_order_is_processed`

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `52 passed`
- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `25 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `167 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T16:36:46Z
- **Focus**: Position-aware SELL risk handling + hedge operator commands.

### Completed

1. Pre-trade risk fix in `tradingagents/graph/paper_execution.py`:
   - non-hedge SELL intents now project signed exposure against current positions.
   - reducing/closing longs is accepted when shorting is blocked.
   - only SELL intents that would create net short exposure are rejected under short-block policy.
   - gross and open-position projections now reflect signed before/after state.

2. New hedge operation commands in `cli/main.py`:
   - `hedge-evaluate` for one-shot deterministic hedge cycle output.
   - `hedge-status` for persisted hedge state and recent hedge order visibility.

3. Tests:
   - `tests/test_paper_execution.py`: added SELL-reduce and flip-to-short coverage.
   - `tests/test_cli_hedging.py`: added CLI coverage for `hedge-evaluate` and `hedge-status`.

4. Docs:
   - updated `README.md` command cookbook with hedge operations commands.

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `27 passed`
- `./.venv/bin/python -m pytest tests/test_cli_hedging.py -q` -> `6 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `171 passed`
- smoke:
  - `./.venv/bin/python -m cli.main hedge-evaluate --format table`
  - `./.venv/bin/python -m cli.main hedge-status --format table`

## Incremental Update

- **Timestamp**: 2026-02-07T15:21:17Z
- **Focus**: Step 2.4 execution-ops hardening (stale orders + broker-shadow sync).

### Completed

1. Added stale open-order manager:
   - `manage-open-orders` command in `cli/main.py`
   - stale detection, cancel-only and cancel+replace flows
   - per-symbol retry cap enforcement
   - artifacts:
     - `eval_results/live_execution/open_orders/<date>/manage_open_orders_<time>.json`
     - `eval_results/live_execution/latest_open_orders_management.json`
   - audit events:
     - `OPEN_ORDER_STALE_DETECTED`
     - `OPEN_ORDER_CANCELED`
     - `OPEN_ORDER_REPLACED`
     - `OPEN_ORDER_RETRY_EXHAUSTED`
     - `OPEN_ORDER_MANAGEMENT_COMPLETED`

2. Added broker-to-shadow auto-heal sync:
   - `sync-positions-from-broker` command in `cli/main.py`
   - optional `--drop-missing` mode
   - preserves local strategy metadata when symbol already exists
   - artifacts:
     - `eval_results/live_execution/positions_sync/<date>/sync_positions_<time>.json`
     - `eval_results/live_execution/latest_positions_sync.json`
   - audit event:
     - `LIVE_POSITIONS_SHADOW_SYNCED`

3. Added defaults and env documentation:
   - `tradingagents/default_config.py`:
     - `execution_open_orders_max_age_minutes`
     - `execution_open_orders_replace_stale`
     - `execution_open_orders_replacement_order_type`
     - `execution_open_orders_limit_price_offset_bps`
     - `execution_open_orders_max_retries_per_symbol_per_day`
   - `.env.example`:
     - `EXECUTION_OPEN_ORDERS_*` keys

4. Updated operator docs in `README.md`:
   - runbook examples for `manage-open-orders` and `sync-positions-from-broker`

5. Added tests:
   - `tests/test_cli_dealflow.py`
     - stale-order dry-run command coverage
     - positions-sync apply command coverage

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `46 passed`
- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `22 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `154 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T15:39:49Z
- **Focus**: Full execution-sync integration pass to run open-order management + broker-shadow sync in one loop.

### Completed

1. Enhanced `execution-sync` in `cli/main.py` with optional stale-order manager integration:
   - `--manage-open-orders/--skip-manage-open-orders`
   - `--manage-open-orders-apply/--manage-open-orders-dry-run`
   - stale/replacement parameter surface (`open-orders-*` flags).
   - persists open-order management artifact and prints cycle summary.

2. Enhanced `execution-sync` in `cli/main.py` with optional broker->shadow sync:
   - `--broker-positions-snapshot-path`
   - `--sync-shadow-positions/--skip-shadow-positions-sync`
   - `--sync-shadow-positions-apply/--sync-shadow-positions-dry-run`
   - `--sync-shadow-positions-drop-missing/--sync-shadow-positions-keep-missing`
   - persists positions sync artifact and prints cycle summary.

3. Added new defaults in `tradingagents/default_config.py`:
   - `execution_sync_manage_open_orders`
   - `execution_sync_manage_open_orders_apply`
   - `execution_sync_sync_shadow_positions`
   - `execution_sync_sync_shadow_positions_apply`

4. Updated `.env.example` with matching `EXECUTION_SYNC_*` keys.

5. Updated operator runbook in `README.md`:
   - added one-command full safety cycle example for `execution-sync`.

6. Added tests in `tests/test_cli_dealflow.py`:
   - execution-sync wiring for open-order manager + shadow sync.
   - apply-mode snapshot refresh behavior after open-order management.

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `48 passed`
- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `22 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `156 passed`

## Incremental Update

- **Timestamp**: 2026-02-07T15:50:12Z
- **Focus**: Complete one-command workflow automation for operations.

### Completed

1. Added `workflow-run` command in `cli/main.py`:
   - End-to-end execution chain:
     - orchestrate
     - analyze-batch
     - portfolio-plan
     - execute-paper (optional)
     - execution-sync (optional)
   - Supports strict stage-failure statuses and skip semantics.
   - Supports execution/sync controls (including open-order manager and shadow sync via execution-sync options).

2. Added helper utilities in `cli/main.py`:
   - `_run_cli_subcommand_json(...)` for consistent subprocess JSON execution.
   - `_persist_workflow_run(...)` for workflow artifact persistence.

3. Added workflow artifact storage:
   - `eval_results/live_execution/workflow/<date>/workflow_run_<time>.json`
   - `eval_results/live_execution/latest_workflow_run.json`

4. Updated `README.md`:
   - Added full one-command automation run example.

5. Added tests in `tests/test_cli_dealflow.py`:
   - orchestration-skip path for workflow.
   - full success path validating analyze/plan/execute/sync wiring.

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `50 passed`
- `./.venv/bin/python -m pytest tests/ -v` -> `158 passed`
- `./.venv/bin/python -m cli.main workflow-run --help` verified options.

## Completed

1. Added discovered-symbol sanitization in `tradingagents/dealflow/pipeline.py`.
   - configurable min/max symbol length gates
   - configurable denylist filter
   - strict ticker-shape validation before dynamic-universe promotion

2. Added connector timeout/retry protections in `tradingagents/dealflow/pipeline.py`.
   - `DEALFLOW_CONNECTOR_TIMEOUT_SECONDS` (default `45`)
   - `DEALFLOW_CONNECTOR_MAX_ATTEMPTS` (default `2`)

3. Hardened yfinance-dependent signal collectors:
   - `tradingagents/dealflow/sources/price_momentum.py`
     - batched download flow
     - suppressed noisy yfinance stderr/stdout
     - safer partial-data behavior
   - `tradingagents/dealflow/sources/macro.py`
     - suppressed yfinance noise
     - non-threaded fetch for stability

4. Expanded default shortlist target:
   - `dealflow_top_k`: `20 -> 30`
   - lane quotas: `12/8 -> 18/12` (`CORE/MOMENTUM`)

5. Updated operator command examples:
   - `README.md` source examples now use `--top-k 30`.

6. Added/updated tests:
   - `tests/test_dealflow_momentum.py`
   - `tests/test_cli_dealflow.py`

## Validation

- `./.venv/bin/python -m pytest tests/ -q`
- Result: `117 passed`

## Next Suggested Actions

1. Run live smoke with current APIs:
   - `./.venv/bin/python -m cli.main source --date 2026-02-06 --trigger manual --profile daily --format table`
2. Confirm lane distribution and candidate count behavior at Top-30 defaults.
3. Continue Step 2 execution hardening (idempotency, pre-trade risk checks, brokerage adapter boundary).

---

## Incremental Update

- **Timestamp**: 2026-02-07T15:05:00Z
- **Focus**: Improve visibility between broker state and local ledgers (operator diagnostics).

### Completed

1. Added Alpaca position snapshot fetcher in `tradingagents/graph/paper_execution.py`:
   - `fetch_alpaca_positions_snapshot(...)`
   - persists broker positions snapshot to configurable output path.

2. Added CLI command `pull-broker-positions` in `cli/main.py`:
   - fetches current broker positions and saves snapshot artifact.

3. Added CLI command `live-positions` in `cli/main.py`:
   - renders live/shadow positions ledger used by reconciliation.

4. Added startup warning in `cli/main.py`:
   - one-time warning when `paper_positions_path` and `live_positions_shadow_path` differ.

5. Config + docs:
   - added `live_broker_positions_snapshot_path` in `tradingagents/default_config.py`
   - added `LIVE_BROKER_POSITIONS_SNAPSHOT_PATH` in `.env.example`
   - updated command examples in `README.md`.

6. Tests added/updated:
   - `tests/test_paper_execution.py`: position snapshot fetch coverage
   - `tests/test_cli_dealflow.py`: `pull-broker-positions` and `live-positions` command coverage

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `22 passed`
- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `42 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `150 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T15:04:05Z
- **Focus**: Add per-symbol broker-vs-shadow drift report for execution diagnostics.

### Completed

1. Added `positions-drift` command in `cli/main.py`.
   - compares broker positions snapshot (`pull-broker-positions` artifact) with live shadow positions.
   - computes per-symbol qty drift + notional drift and outputs summary/table/json.
   - supports `--fail-on-drift` for non-zero exit on mismatch.

2. Added drift artifact persistence in `cli/main.py`.
   - `eval_results/live_execution/positions_drift/<date>/positions_drift_<time>.json`
   - `eval_results/live_execution/latest_positions_drift.json`

3. Added audit event:
   - `LIVE_POSITIONS_DRIFT_EVALUATED`

4. Updated docs:
   - `README.md` now includes `positions-drift` command examples.

5. Added tests:
   - `tests/test_cli_dealflow.py`:
     - drift detection JSON flow
     - `--fail-on-drift` exit code behavior

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `44 passed`
- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `22 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `152 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T14:42:13Z
- **Focus**: Step 2.2 + 2.3 completed together (deterministic exits + execution-readiness gate).

### Completed

1. Added deterministic exit planning in `tradingagents/graph/paper_execution.py`:
   - `build_exit_execution_plan(...)` with stop-loss, take-profit, max-hold, trailing-stop rules.
   - Skip protections for active exits, low-notional positions, and whole-share zero-qty cases.
   - Exit intents now include `intent_category=EXIT`, `exit_rule`, and `exit_reason`.

2. Added broker-connected readiness gate in `tradingagents/graph/paper_execution.py`:
   - `evaluate_execution_readiness(...)` checks credentials, account state, stale pending orders, open-order matching, and position drift.
   - Deterministic pass/block output with blockers and warnings.

3. Wired CLI command surface in `cli/main.py`:
   - Enhanced `execution-sync`:
     - `--apply-exits/--skip-exits`
     - `--require-ready/--skip-ready-check`
     - readiness and exit rule threshold controls.
   - Added `manage-exits` command (dry-run or submit).
   - Added `execution-readiness` command (with `--fail-on-blocked` support).

4. Expanded configuration/env surface:
   - `tradingagents/default_config.py` now includes `execution_exit_*` and `execution_readiness_*` knobs.
   - `.env.example` now documents the same keys.

5. Added regression tests:
   - `tests/test_paper_execution.py`: exit signal generation + readiness pass/fail coverage.
   - `tests/test_cli_dealflow.py`: sync readiness-block behavior, manage-exits JSON path, readiness fail-on-blocked exit code.

6. Updated operator docs:
   - `README.md` now includes `manage-exits`, `execution-readiness`, and `execution-sync --apply-exits --require-ready` runbook commands.

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `61 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `147 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T01:42:07Z
- **Focus**: Batch execution semantics update: deep for selected + quick for non-selected.

### Completed

1. Updated `analyze-batch` in `cli/main.py`:
   - default behavior is now dual-pass:
     - `DEEP` mode for `selected_for_deep=true`
     - `QUICK` mode for remaining queue items
   - new flags:
     - `--include-unselected/--selected-only`
     - `--quick-unselected/--deep-unselected`
   - summary now includes:
     - per-item `analysis_mode`
     - run-level `analysis_mode_counts`

2. Updated quick scoring reliability in `cli/main.py`:
   - `score` command now resolves non-interactive provider/model/backend consistently with queue runs.

3. Updated batch results rendering:
   - Added `Mode` column to batch table output.

4. Added/updated tests:
   - New test for default quick-mode behavior on unselected queue items.
   - Selected-only batch test now explicitly uses `--selected-only`.

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `29 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `118 passed`
- CLI smoke:
  - `./.venv/bin/python -m cli.main analyze-batch --queue-date 2026-02-06 --dry-run --format json`
  - Confirms `analysis_mode_counts` and mixed `DEEP/QUICK` item modes in output.

---

## Incremental Update

- **Timestamp**: 2026-02-07T02:10:17Z
- **Focus**: Step 2 execution hardening with deterministic pre-trade risk checks.

### Completed

1. Added risk engine in `tradingagents/graph/paper_execution.py`:
   - `evaluate_pretrade_risk(...)`
   - deterministic acceptance/rejection of order intents before execution
   - reason-coded rejections + projected exposure tracking

2. Upgraded `execute-paper` in `cli/main.py`:
   - executes only pre-trade accepted intents
   - emits `risk_check` summary in command output
   - persists risk artifact with `_persist_pretrade_risk(...)`
   - added runtime controls:
     - `--max-gross-exposure-pct`
     - `--max-single-position-pct`
     - `--max-open-positions`
     - `--max-new-orders-per-run`
     - `--block-shorts/--allow-shorts`

3. Expanded audit events in `cli/main.py`:
   - `PAPER_PRETRADE_RISK_CHECK`
   - `PAPER_ORDER_REJECTED_RISK`

4. Added Step 2 default config knobs in `tradingagents/default_config.py`:
   - `execution_max_gross_exposure_pct`
   - `execution_max_single_position_pct`
   - `execution_max_open_positions`
   - `execution_max_new_orders_per_run`
   - `execution_block_short_orders`

5. Updated operator command docs in `README.md`:
   - dual-pass analyze-batch command set
   - execute-paper risk override examples

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `34 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `120 passed`
- `./.venv/bin/python -m cli.main execute-paper --format json` -> success (risk payload + risk artifact path present)

---

## Incremental Update

- **Timestamp**: 2026-02-07T04:05:34Z
- **Focus**: Fix empty `portfolio-plan` outputs caused by dry-run summary selection and missing reference prices.

### Completed

1. Updated `cli/main.py` `_load_batch_summary(...)` to prefer latest non-dry-run batch artifacts.
2. Updated `tradingagents/graph/paper_execution.py`:
   - Added market-price fallback for missing `analysis_report` price fields.
   - Added `reference_price_source` on generated order intents.
3. Added tests:
   - non-dry-run summary preference (`tests/test_cli_dealflow.py`)
   - market fallback price usage (`tests/test_paper_execution.py`)

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `36 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `122 passed`
- Live smoke:
  - `portfolio-plan --queue-date 2026-02-06` -> `orders=4`
  - `execute-paper --format json` -> `executed_orders=4`, `risk_check.status=PASS`

---

## Incremental Update

- **Timestamp**: 2026-02-07T04:14:50Z
- **Focus**: Rebalance/delta execution to prevent repeated full-size re-buys.

### Completed

1. Added `build_rebalance_execution_plan(...)` in `tradingagents/graph/paper_execution.py`.
   - converts target intents to delta intents vs current positions
   - supports tolerance skipping and optional close-missing generation
2. Updated `execute-paper` in `cli/main.py`:
   - new flags:
     - `--rebalance-to-target/--full-size-intents`
     - `--min-rebalance-notional-usd`
     - `--close-missing-positions/--keep-missing-positions`
   - rebalance now runs before risk checks
3. Added Step 2 defaults in `tradingagents/default_config.py`:
   - `execution_rebalance_to_target=true`
   - `execution_min_rebalance_notional_usd=100.0`
   - `execution_close_missing_positions=false`
4. Added tests:
   - rebalance skip-within-tolerance
   - rebalance delta order generation
   - CLI `execute-paper` rebalance payload assertion

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `38 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `124 passed`
- repeated `execute-paper` now no-ops cleanly (`NO_ORDERS`) when positions already match targets.

---

## Incremental Update

- **Timestamp**: 2026-02-07T04:22:39Z
- **Focus**: Step 2 live execution scaffold (queue-only) and CLI live-mode wiring.

### Completed

1. Added `LiveExecutionAdapter` in `tradingagents/graph/paper_execution.py`.
   - queue-only submission mode for live intents
   - no broker fills in scaffold mode
   - preserves idempotent duplicate skipping by `order_intent_id`

2. Updated `execute-paper` in `cli/main.py`.
   - live mode now uses live outbox + shadow positions paths
   - output now includes `submitted_orders` in addition to `executed_orders`

3. Added defaults in `tradingagents/default_config.py`.
   - `live_execution_outbox_path`
   - `live_positions_shadow_path`

4. Added/updated tests.
   - live-mode CLI path/assertions in `tests/test_cli_dealflow.py`
   - live adapter idempotency behavior in `tests/test_paper_execution.py`

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `40 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `126 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T11:39:49Z
- **Focus**: Step 2 reconciliation loop for live-mode order lifecycle.

### Completed

1. Added `reconcile_live_execution(...)` in `tradingagents/graph/paper_execution.py`.
   - reconciles broker snapshot rows to live outbox orders by intent/client ids
   - updates normalized statuses in outbox
   - applies only fill deltas to shadow positions (idempotent on reruns)
   - persists applied fill events to `live_execution` fill ledger

2. Added CLI command in `cli/main.py`:
   - `reconcile-execution --broker-snapshot-path ...`
   - emits reconciliation artifacts + summary
   - emits audit events (`LIVE_RECONCILIATION_COMPLETED`, `LIVE_ORDER_RECONCILED`, `LIVE_FILL_APPLIED`)

3. Added config/env keys:
   - `live_execution_fills_path`
   - `live_broker_orders_snapshot_path`

4. Updated command docs in `README.md` with reconciliation examples.

5. Added tests:
   - `tests/test_paper_execution.py`:
     - live reconciliation idempotency test
     - partial-fill delta reconciliation test
   - `tests/test_cli_dealflow.py`:
     - `reconcile-execution` command JSON path test

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `43 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `129 passed`
- smoke:
  - `./.venv/bin/python -m cli.main reconcile-execution --broker-snapshot-path <snapshot.json> --format json`

---

## Incremental Update

- **Timestamp**: 2026-02-07T12:25:21Z
- **Focus**: Alpaca-first broker adapter integration for Step 2.

### Completed

1. Added Alpaca execution modes in `tradingagents/graph/paper_execution.py`.
   - `alpaca-paper`
   - `alpaca-live`
   - direct order submission to `/v2/orders` with idempotent local outbox tracking

2. Added broker snapshot pull utility in `tradingagents/graph/paper_execution.py`.
   - `fetch_alpaca_orders_snapshot(...)` pulls orders and persists reconciliation-ready snapshot JSON.

3. Added new CLI command in `cli/main.py`.
   - `pull-broker-orders --broker alpaca --mode alpaca-paper|alpaca-live`
   - persists to `live_broker_orders_snapshot_path` for direct use by `reconcile-execution`

4. Updated `execute-paper` mode routing in `cli/main.py`.
   - Alpaca modes now use live outbox + shadow positions paths.

5. Expanded config/docs.
   - `tradingagents/default_config.py`: Alpaca key/base-url/timeout settings.
   - `.env.example`: Alpaca key/base-url templates.
   - `README.md`: Alpaca execute + pull + reconcile command flow.

6. Added tests.
   - `tests/test_paper_execution.py`:
     - Alpaca submission + duplicate skip
     - Alpaca snapshot fetch
   - `tests/test_cli_dealflow.py`:
     - alpaca execution mode path selection
     - pull-broker-orders command JSON output

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `47 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `133 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T12:39:31Z
- **Focus**: Alpaca base URL misconfiguration hardening.

### Completed

1. Root-cause identified:
   - `ALPACA_API_BASE_URL` included `/v2`, producing `.../v2/v2/orders` and HTTP 404.
2. Added base URL sanitizer:
   - `_normalize_alpaca_base_url(...)` in `tradingagents/graph/paper_execution.py`
   - strips trailing `/v2` before request path composition.
3. Extended test coverage:
   - `tests/test_paper_execution.py` now validates `/v2` input normalizes to correct orders endpoint URL.

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `47 passed`
- `./.venv/bin/python -m cli.main pull-broker-orders --broker alpaca --mode alpaca-paper --status all --limit 5 --format json` -> success

---

## Incremental Update

- **Timestamp**: 2026-02-07T12:54:08Z
- **Focus**: Whole-share quantity enforcement for Alpaca order flow.

### Completed

1. Updated Alpaca submission logic in `tradingagents/graph/paper_execution.py`.
   - applies whole-share coercion by default via `ALPACA_ENFORCE_WHOLE_SHARES=true`
   - rounds fractional quantities down (e.g. `37.795 -> 37`)
   - blocks rounded-to-zero intents with explicit error (`WHOLE_SHARE_ROUND_DOWN_TO_ZERO`)

2. Updated config/docs.
   - `tradingagents/default_config.py`: `alpaca_enforce_whole_shares`
   - `.env.example`: `ALPACA_ENFORCE_WHOLE_SHARES`
   - `README.md`: quantity policy note under Alpaca execution command

3. Added test coverage.
   - `tests/test_paper_execution.py`:
     - decimal quantity coercion test (`META 37.795 -> qty 37`)
     - sub-1-share rejection test (no broker POST when rounded qty becomes zero)

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `49 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `135 passed`

---

## Incremental Update

- **Timestamp**: 2026-02-07T13:08:12Z
- **Focus**: Portfolio-plan quantity parity with Alpaca whole-share execution.

### Completed

1. Updated portfolio plan construction in `tradingagents/graph/paper_execution.py`.
   - `build_portfolio_plan(...)` now accepts `enforce_whole_shares`.
   - when enabled, target quantities are rounded down to whole shares before plan persistence.
   - order notional/weight are recomputed from rounded quantity.
   - `quantity_policy` is persisted per order (`WHOLE_SHARES` or `FRACTIONAL_OK`).

2. Updated portfolio plan CLI in `cli/main.py`.
   - added `portfolio-plan --execution-mode` to make quantity policy explicit at plan time.
   - plan generation enables whole-share sizing when mode starts with `alpaca` and `alpaca_enforce_whole_shares=true`.
   - `_render_portfolio_plan_table(...)` now prints integer quantity values for whole-share orders.

3. Updated tests.
   - `tests/test_paper_execution.py`: added whole-share portfolio-plan sizing test.
   - `tests/test_cli_dealflow.py`: added CLI coverage for alpaca whole-share planning mode.

4. Updated runbook docs.
   - `README.md` portfolio-plan command examples now include `--execution-mode alpaca-paper`.

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py tests/test_cli_dealflow.py -q` -> `51 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `137 passed`
- `./.venv/bin/python -m cli.main portfolio-plan --queue-date 2026-02-06 --capital-usd 100000 --max-positions 8 --execution-mode alpaca-paper --format table` -> Qty column renders whole-share values (`3022`, `89`, `77`, `37`).

---

## Incremental Update

- **Timestamp**: 2026-02-07T13:54:50Z
- **Focus**: Step 2 lifecycle automation - one-command execution sync (pull + reconcile + position/PnL refresh).

### Completed

1. Added a new live lifecycle command in `cli/main.py`:
   - `aeternus execution-sync`
   - one cycle does:
     - broker snapshot pull (Alpaca)
     - live reconciliation into outbox/fills/shadow positions
     - mark-to-market refresh for open positions (market value + unrealized PnL)
   - supports modes:
     - `--once`
     - `--loop --interval-sec <n> --max-loops <n>`

2. Added reusable sync/audit helpers in `cli/main.py`:
   - `_run_execution_sync_cycle(...)`
   - `_audit_live_broker_snapshot(...)`
   - `_audit_live_reconciliation(...)`

3. Added position/PnL refresh in `tradingagents/graph/paper_execution.py`:
   - `refresh_positions_market_snapshot(...)`
   - updates per-position fields:
     - `last_mark_price`
     - `market_value_usd`
     - `unrealized_pnl_usd`
     - `unrealized_return_pct`
     - `mark_to_market_at`

4. Updated operator docs in `README.md`:
   - added one-shot and looping `execution-sync` commands.

5. Added tests:
   - `tests/test_cli_dealflow.py`
     - `execution-sync` one-shot JSON path
     - `execution-sync` loop mode honoring max loops + interval
   - `tests/test_paper_execution.py`
     - market snapshot refresh updates valuation and unrealized PnL fields deterministically

### Validation

- `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `17 passed`
- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `37 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `140 passed`
- Live smoke:
  - `./.venv/bin/python -m cli.main execution-sync --broker alpaca --mode alpaca-paper --status all --limit 5 --once --format table`
  - output: cycle completed with snapshot + reconciliation + position refresh metrics.

## Incremental Update

- **Timestamp**: 2026-02-07T20:48:37Z
- **Focus**: Pre-submit position-parity guardrail + portfolio-plan hedge/risk visibility.

### Completed

1. `execute-paper` parity gate hardening (`cli/main.py`):
   - Added broker-vs-shadow parity evaluation before alpaca submission.
   - Added parity control flags and parity payload in execution result.
   - Blocking mode converts accepted intents to risk rejections when parity drift/error is detected.

2. Audit wiring:
   - Added `LIVE_POSITION_PARITY_EVALUATED`.
   - Added `PAPER_EXECUTION_BLOCKED_POSITION_PARITY` when submission is blocked.

3. Portfolio manager visibility:
   - Added `build_portfolio_risk_summary(...)`.
   - `portfolio-plan` now persists `portfolio_risk_summary` and renders the risk/hedge recommendation panel in terminal output.

4. Config and docs:
   - Added new parity defaults in `tradingagents/default_config.py`.
   - Updated `README.md` with parity behavior and warn-only example.

5. Tests:
   - Extended `tests/test_cli_dealflow.py` for parity payload and blocking behavior.

### Validation

- `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `53 passed`
- `./.venv/bin/python -m pytest tests/ -q` -> `172 passed`

## 2026-02-07T21:11:52Z - P0 readiness self-healing in execution-sync

- Added readiness auto-remediation path in `cli/main.py` `execution-sync`:
  - auto-cancel stale pending orders when stale/open-order-match checks fail.
  - auto-sync broker->shadow positions when drift check fails.
  - immediate readiness re-evaluation in same cycle.
- Added CLI toggles:
  - `--auto-remediate-readiness/--no-auto-remediate-readiness`
  - `--auto-remediate-drop-missing/--auto-remediate-keep-missing`
- Added config/env keys:
  - `execution_sync_auto_remediate_readiness`
  - `execution_sync_auto_remediate_drop_missing`
  - `EXECUTION_SYNC_AUTO_REMEDIATE_READINESS`
  - `EXECUTION_SYNC_AUTO_REMEDIATE_DROP_MISSING`
- Added test:
  - `tests/test_cli_dealflow.py::test_execution_sync_auto_remediates_readiness_blockers`
- Validation:
  - `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `54 passed`
  - `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `27 passed`
  - `./.venv/bin/python -m pytest tests/ -q` -> `173 passed`

## 2026-02-07T21:28:07Z - Step 2 P0 closure: live reconcile now closes positions + updates outcomes

- `reconcile_live_execution(...)` now persists closed-position events to `eval_results/live_execution/closed_trades.json` (configurable via `LIVE_EXECUTION_CLOSED_TRADES_PATH`).
- Reconciled fills that fully close or cross existing positions now:
  - emit close events,
  - update linked rating outcomes through `TrackRecord.update_outcome(...)`,
  - return closed/outcome metrics in reconcile payload.
- `reconcile-execution` and `execution-sync` now accept `--closed-trades-path`.
- Reconcile audit now includes close/outcome metrics and emits `LIVE_POSITION_CLOSED` events.
- Added regression test for close/outcome propagation and idempotency.
- Validation:
  - `./.venv/bin/python -m pytest tests/test_paper_execution.py -q` -> `28 passed`
  - `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `54 passed`
  - `./.venv/bin/python -m pytest tests/ -q` -> `174 passed`

## 2026-02-07T21:43:00Z - Step 2 P0 final hardening: live vs paper positions path split

- Updated default live shadow path to `eval_results/live_execution/positions_shadow.json` in `tradingagents/default_config.py`.
- Updated CLI fallbacks to use live shadow path for live/alpaca operations.
- Fixed startup warning: it now warns only when paper and live ledgers are identical (true contamination risk).
- Updated `.env.example` to reflect live shadow default.
- Validation:
  - `./.venv/bin/python -m pytest tests/test_cli_dealflow.py -q` -> `54 passed`
  - `./.venv/bin/python -m pytest tests/ -q` -> `174 passed`
  - CLI warning smoke checks passed (warning only on forced same-path configuration).
